// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: upp/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "upp.v1";

/** Standardized error codes across all providers. */
export enum ErrorCode {
  ERROR_CODE_UNSPECIFIED = 0,
  /** ERROR_CODE_INVALID_ARGUMENT - Client errors (4xx equivalent) */
  ERROR_CODE_INVALID_ARGUMENT = 1,
  /** ERROR_CODE_NOT_FOUND - Market/order/resource not found */
  ERROR_CODE_NOT_FOUND = 2,
  /** ERROR_CODE_ALREADY_EXISTS - Duplicate order (idempotency) */
  ERROR_CODE_ALREADY_EXISTS = 3,
  /** ERROR_CODE_PERMISSION_DENIED - Insufficient permissions */
  ERROR_CODE_PERMISSION_DENIED = 4,
  /** ERROR_CODE_UNAUTHENTICATED - Missing or invalid credentials */
  ERROR_CODE_UNAUTHENTICATED = 5,
  /** ERROR_CODE_RATE_LIMITED - Too many requests */
  ERROR_CODE_RATE_LIMITED = 6,
  /** ERROR_CODE_INSUFFICIENT_FUNDS - Not enough balance */
  ERROR_CODE_INSUFFICIENT_FUNDS = 7,
  /** ERROR_CODE_MARKET_CLOSED - Market not accepting orders */
  ERROR_CODE_MARKET_CLOSED = 8,
  /** ERROR_CODE_ORDER_REJECTED - Order violates exchange rules */
  ERROR_CODE_ORDER_REJECTED = 9,
  /** ERROR_CODE_REGION_RESTRICTED - User's region not eligible */
  ERROR_CODE_REGION_RESTRICTED = 10,
  /** ERROR_CODE_INTERNAL - Server errors (5xx equivalent) */
  ERROR_CODE_INTERNAL = 50,
  /** ERROR_CODE_UNAVAILABLE - Provider temporarily down */
  ERROR_CODE_UNAVAILABLE = 51,
  /** ERROR_CODE_TIMEOUT - Request timed out */
  ERROR_CODE_TIMEOUT = 52,
  /** ERROR_CODE_PROVIDER_ERROR - Upstream provider error */
  ERROR_CODE_PROVIDER_ERROR = 53,
  UNRECOGNIZED = -1,
}

export function errorCodeFromJSON(object: any): ErrorCode {
  switch (object) {
    case 0:
    case "ERROR_CODE_UNSPECIFIED":
      return ErrorCode.ERROR_CODE_UNSPECIFIED;
    case 1:
    case "ERROR_CODE_INVALID_ARGUMENT":
      return ErrorCode.ERROR_CODE_INVALID_ARGUMENT;
    case 2:
    case "ERROR_CODE_NOT_FOUND":
      return ErrorCode.ERROR_CODE_NOT_FOUND;
    case 3:
    case "ERROR_CODE_ALREADY_EXISTS":
      return ErrorCode.ERROR_CODE_ALREADY_EXISTS;
    case 4:
    case "ERROR_CODE_PERMISSION_DENIED":
      return ErrorCode.ERROR_CODE_PERMISSION_DENIED;
    case 5:
    case "ERROR_CODE_UNAUTHENTICATED":
      return ErrorCode.ERROR_CODE_UNAUTHENTICATED;
    case 6:
    case "ERROR_CODE_RATE_LIMITED":
      return ErrorCode.ERROR_CODE_RATE_LIMITED;
    case 7:
    case "ERROR_CODE_INSUFFICIENT_FUNDS":
      return ErrorCode.ERROR_CODE_INSUFFICIENT_FUNDS;
    case 8:
    case "ERROR_CODE_MARKET_CLOSED":
      return ErrorCode.ERROR_CODE_MARKET_CLOSED;
    case 9:
    case "ERROR_CODE_ORDER_REJECTED":
      return ErrorCode.ERROR_CODE_ORDER_REJECTED;
    case 10:
    case "ERROR_CODE_REGION_RESTRICTED":
      return ErrorCode.ERROR_CODE_REGION_RESTRICTED;
    case 50:
    case "ERROR_CODE_INTERNAL":
      return ErrorCode.ERROR_CODE_INTERNAL;
    case 51:
    case "ERROR_CODE_UNAVAILABLE":
      return ErrorCode.ERROR_CODE_UNAVAILABLE;
    case 52:
    case "ERROR_CODE_TIMEOUT":
      return ErrorCode.ERROR_CODE_TIMEOUT;
    case 53:
    case "ERROR_CODE_PROVIDER_ERROR":
      return ErrorCode.ERROR_CODE_PROVIDER_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ErrorCode.UNRECOGNIZED;
  }
}

export function errorCodeToJSON(object: ErrorCode): string {
  switch (object) {
    case ErrorCode.ERROR_CODE_UNSPECIFIED:
      return "ERROR_CODE_UNSPECIFIED";
    case ErrorCode.ERROR_CODE_INVALID_ARGUMENT:
      return "ERROR_CODE_INVALID_ARGUMENT";
    case ErrorCode.ERROR_CODE_NOT_FOUND:
      return "ERROR_CODE_NOT_FOUND";
    case ErrorCode.ERROR_CODE_ALREADY_EXISTS:
      return "ERROR_CODE_ALREADY_EXISTS";
    case ErrorCode.ERROR_CODE_PERMISSION_DENIED:
      return "ERROR_CODE_PERMISSION_DENIED";
    case ErrorCode.ERROR_CODE_UNAUTHENTICATED:
      return "ERROR_CODE_UNAUTHENTICATED";
    case ErrorCode.ERROR_CODE_RATE_LIMITED:
      return "ERROR_CODE_RATE_LIMITED";
    case ErrorCode.ERROR_CODE_INSUFFICIENT_FUNDS:
      return "ERROR_CODE_INSUFFICIENT_FUNDS";
    case ErrorCode.ERROR_CODE_MARKET_CLOSED:
      return "ERROR_CODE_MARKET_CLOSED";
    case ErrorCode.ERROR_CODE_ORDER_REJECTED:
      return "ERROR_CODE_ORDER_REJECTED";
    case ErrorCode.ERROR_CODE_REGION_RESTRICTED:
      return "ERROR_CODE_REGION_RESTRICTED";
    case ErrorCode.ERROR_CODE_INTERNAL:
      return "ERROR_CODE_INTERNAL";
    case ErrorCode.ERROR_CODE_UNAVAILABLE:
      return "ERROR_CODE_UNAVAILABLE";
    case ErrorCode.ERROR_CODE_TIMEOUT:
      return "ERROR_CODE_TIMEOUT";
    case ErrorCode.ERROR_CODE_PROVIDER_ERROR:
      return "ERROR_CODE_PROVIDER_ERROR";
    case ErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Sort direction for list queries. */
export enum SortDirection {
  SORT_DIRECTION_UNSPECIFIED = 0,
  SORT_DIRECTION_ASC = 1,
  SORT_DIRECTION_DESC = 2,
  UNRECOGNIZED = -1,
}

export function sortDirectionFromJSON(object: any): SortDirection {
  switch (object) {
    case 0:
    case "SORT_DIRECTION_UNSPECIFIED":
      return SortDirection.SORT_DIRECTION_UNSPECIFIED;
    case 1:
    case "SORT_DIRECTION_ASC":
      return SortDirection.SORT_DIRECTION_ASC;
    case 2:
    case "SORT_DIRECTION_DESC":
      return SortDirection.SORT_DIRECTION_DESC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SortDirection.UNRECOGNIZED;
  }
}

export function sortDirectionToJSON(object: SortDirection): string {
  switch (object) {
    case SortDirection.SORT_DIRECTION_UNSPECIFIED:
      return "SORT_DIRECTION_UNSPECIFIED";
    case SortDirection.SORT_DIRECTION_ASC:
      return "SORT_DIRECTION_ASC";
    case SortDirection.SORT_DIRECTION_DESC:
      return "SORT_DIRECTION_DESC";
    case SortDirection.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * High-precision timestamp for trading events.
 * Always UTC. Nanosecond precision for orderbook/trade events.
 */
export interface Timestamp {
  /** Unix epoch seconds */
  seconds: number;
  /** Sub-second nanoseconds [0, 999999999] */
  nanos: number;
}

/** Time range for queries and filters. */
export interface TimeRange {
  start: Timestamp | undefined;
  end: Timestamp | undefined;
}

/**
 * Monetary amount with explicit currency.
 * Uses string to avoid floating-point precision issues in financial calculations.
 */
export interface Money {
  /** Decimal string, e.g. "63.50" */
  amount: string;
  /** ISO 4217 code ("USD") or crypto symbol ("USDC") */
  currency: string;
}

/**
 * Probability value [0.0, 1.0] representing market-implied likelihood.
 * Stored as basis points internally (0-10000) for precision.
 */
export interface Probability {
  /** 6300 = 63.00% = 0.6300 */
  basisPoints: number;
}

/**
 * Price in the provider's native format, normalized to UPP.
 * Prediction market prices always represent probabilities.
 */
export interface Price {
  /** Decimal string, e.g. "0.63" */
  value: string;
  /** Settlement currency */
  currency: string;
}

/**
 * Cursor-based pagination request.
 * Cursor-based (not offset-based) for consistency under real-time inserts.
 */
export interface PaginationRequest {
  /** Max items per page [1, 1000], default 50 */
  limit: number;
  /** Opaque cursor from previous response */
  cursor: string;
}

/** Cursor-based pagination response. */
export interface PaginationResponse {
  /** Cursor for next page (empty if no more) */
  cursor: string;
  /** Whether more results exist */
  hasMore: boolean;
  /** Total count (if available, -1 if unknown) */
  total: number;
}

/** Standardized error response across all UPP services. */
export interface Error {
  /** Machine-readable error code */
  code: ErrorCode;
  /** Human-readable description */
  message: string;
  /** Which provider originated the error */
  provider: string;
  /** Correlation ID for debugging */
  requestId: string;
  /** Additional error context */
  details: { [key: string]: string };
}

export interface Error_DetailsEntry {
  key: string;
  value: string;
}

/**
 * Universal Market Identifier.
 * Format: "upp:{provider}:{native_id}"
 * Enables cross-provider market matching and portfolio aggregation.
 */
export interface UniversalMarketId {
  /** Provider domain, e.g. "kalshi.com" */
  provider: string;
  /** Provider's native market ID */
  nativeId: string;
  /** Computed: "upp:{provider}:{native_id}" */
  fullId: string;
}

/** Provider identifier with metadata. */
export interface ProviderId {
  /** e.g. "kalshi.com", "polymarket.com" */
  domain: string;
  /** Human-readable name */
  name: string;
}

function createBaseTimestamp(): Timestamp {
  return { seconds: 0, nanos: 0 };
}

export const Timestamp: MessageFns<Timestamp> = {
  encode(message: Timestamp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seconds !== 0) {
      writer.uint32(8).int64(message.seconds);
    }
    if (message.nanos !== 0) {
      writer.uint32(16).int32(message.nanos);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Timestamp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestamp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seconds = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nanos = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Timestamp {
    return {
      seconds: isSet(object.seconds) ? globalThis.Number(object.seconds) : 0,
      nanos: isSet(object.nanos) ? globalThis.Number(object.nanos) : 0,
    };
  },

  toJSON(message: Timestamp): unknown {
    const obj: any = {};
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    if (message.nanos !== 0) {
      obj.nanos = Math.round(message.nanos);
    }
    return obj;
  },

  create(base?: DeepPartial<Timestamp>): Timestamp {
    return Timestamp.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Timestamp>): Timestamp {
    const message = createBaseTimestamp();
    message.seconds = object.seconds ?? 0;
    message.nanos = object.nanos ?? 0;
    return message;
  },
};

function createBaseTimeRange(): TimeRange {
  return { start: undefined, end: undefined };
}

export const TimeRange: MessageFns<TimeRange> = {
  encode(message: TimeRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      Timestamp.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.end !== undefined) {
      Timestamp.encode(message.end, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.end = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRange {
    return {
      start: isSet(object.start) ? Timestamp.fromJSON(object.start) : undefined,
      end: isSet(object.end) ? Timestamp.fromJSON(object.end) : undefined,
    };
  },

  toJSON(message: TimeRange): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = Timestamp.toJSON(message.start);
    }
    if (message.end !== undefined) {
      obj.end = Timestamp.toJSON(message.end);
    }
    return obj;
  },

  create(base?: DeepPartial<TimeRange>): TimeRange {
    return TimeRange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TimeRange>): TimeRange {
    const message = createBaseTimeRange();
    message.start = (object.start !== undefined && object.start !== null)
      ? Timestamp.fromPartial(object.start)
      : undefined;
    message.end = (object.end !== undefined && object.end !== null) ? Timestamp.fromPartial(object.end) : undefined;
    return message;
  },
};

function createBaseMoney(): Money {
  return { amount: "", currency: "" };
}

export const Money: MessageFns<Money> = {
  encode(message: Money, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.amount !== "") {
      writer.uint32(10).string(message.amount);
    }
    if (message.currency !== "") {
      writer.uint32(18).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Money {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoney();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.amount = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Money {
    return {
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
    };
  },

  toJSON(message: Money): unknown {
    const obj: any = {};
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    return obj;
  },

  create(base?: DeepPartial<Money>): Money {
    return Money.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Money>): Money {
    const message = createBaseMoney();
    message.amount = object.amount ?? "";
    message.currency = object.currency ?? "";
    return message;
  },
};

function createBaseProbability(): Probability {
  return { basisPoints: 0 };
}

export const Probability: MessageFns<Probability> = {
  encode(message: Probability, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.basisPoints !== 0) {
      writer.uint32(8).uint32(message.basisPoints);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Probability {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProbability();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.basisPoints = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Probability {
    return {
      basisPoints: isSet(object.basisPoints)
        ? globalThis.Number(object.basisPoints)
        : isSet(object.basis_points)
        ? globalThis.Number(object.basis_points)
        : 0,
    };
  },

  toJSON(message: Probability): unknown {
    const obj: any = {};
    if (message.basisPoints !== 0) {
      obj.basisPoints = Math.round(message.basisPoints);
    }
    return obj;
  },

  create(base?: DeepPartial<Probability>): Probability {
    return Probability.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Probability>): Probability {
    const message = createBaseProbability();
    message.basisPoints = object.basisPoints ?? 0;
    return message;
  },
};

function createBasePrice(): Price {
  return { value: "", currency: "" };
}

export const Price: MessageFns<Price> = {
  encode(message: Price, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.currency !== "") {
      writer.uint32(18).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Price {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Price {
    return {
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
    };
  },

  toJSON(message: Price): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    return obj;
  },

  create(base?: DeepPartial<Price>): Price {
    return Price.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Price>): Price {
    const message = createBasePrice();
    message.value = object.value ?? "";
    message.currency = object.currency ?? "";
    return message;
  },
};

function createBasePaginationRequest(): PaginationRequest {
  return { limit: 0, cursor: "" };
}

export const PaginationRequest: MessageFns<PaginationRequest> = {
  encode(message: PaginationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== 0) {
      writer.uint32(8).int32(message.limit);
    }
    if (message.cursor !== "") {
      writer.uint32(18).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaginationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaginationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaginationRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      cursor: isSet(object.cursor) ? globalThis.String(object.cursor) : "",
    };
  },

  toJSON(message: PaginationRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.cursor !== "") {
      obj.cursor = message.cursor;
    }
    return obj;
  },

  create(base?: DeepPartial<PaginationRequest>): PaginationRequest {
    return PaginationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PaginationRequest>): PaginationRequest {
    const message = createBasePaginationRequest();
    message.limit = object.limit ?? 0;
    message.cursor = object.cursor ?? "";
    return message;
  },
};

function createBasePaginationResponse(): PaginationResponse {
  return { cursor: "", hasMore: false, total: 0 };
}

export const PaginationResponse: MessageFns<PaginationResponse> = {
  encode(message: PaginationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== "") {
      writer.uint32(10).string(message.cursor);
    }
    if (message.hasMore !== false) {
      writer.uint32(16).bool(message.hasMore);
    }
    if (message.total !== 0) {
      writer.uint32(24).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaginationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaginationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaginationResponse {
    return {
      cursor: isSet(object.cursor) ? globalThis.String(object.cursor) : "",
      hasMore: isSet(object.hasMore)
        ? globalThis.Boolean(object.hasMore)
        : isSet(object.has_more)
        ? globalThis.Boolean(object.has_more)
        : false,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: PaginationResponse): unknown {
    const obj: any = {};
    if (message.cursor !== "") {
      obj.cursor = message.cursor;
    }
    if (message.hasMore !== false) {
      obj.hasMore = message.hasMore;
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create(base?: DeepPartial<PaginationResponse>): PaginationResponse {
    return PaginationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PaginationResponse>): PaginationResponse {
    const message = createBasePaginationResponse();
    message.cursor = object.cursor ?? "";
    message.hasMore = object.hasMore ?? false;
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseError(): Error {
  return { code: 0, message: "", provider: "", requestId: "", details: {} };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.provider !== "") {
      writer.uint32(26).string(message.provider);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    globalThis.Object.entries(message.details).forEach(([key, value]: [string, string]) => {
      Error_DetailsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Error_DetailsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.details[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Error {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      details: isObject(object.details)
        ? (globalThis.Object.entries(object.details) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Error): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.details) {
      const entries = globalThis.Object.entries(message.details) as [string, string][];
      if (entries.length > 0) {
        obj.details = {};
        entries.forEach(([k, v]) => {
          obj.details[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<Error>): Error {
    return Error.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Error>): Error {
    const message = createBaseError();
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.provider = object.provider ?? "";
    message.requestId = object.requestId ?? "";
    message.details = (globalThis.Object.entries(object.details ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseError_DetailsEntry(): Error_DetailsEntry {
  return { key: "", value: "" };
}

export const Error_DetailsEntry: MessageFns<Error_DetailsEntry> = {
  encode(message: Error_DetailsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error_DetailsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError_DetailsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Error_DetailsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Error_DetailsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Error_DetailsEntry>): Error_DetailsEntry {
    return Error_DetailsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Error_DetailsEntry>): Error_DetailsEntry {
    const message = createBaseError_DetailsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseUniversalMarketId(): UniversalMarketId {
  return { provider: "", nativeId: "", fullId: "" };
}

export const UniversalMarketId: MessageFns<UniversalMarketId> = {
  encode(message: UniversalMarketId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.nativeId !== "") {
      writer.uint32(18).string(message.nativeId);
    }
    if (message.fullId !== "") {
      writer.uint32(26).string(message.fullId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UniversalMarketId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUniversalMarketId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nativeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fullId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UniversalMarketId {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      nativeId: isSet(object.nativeId)
        ? globalThis.String(object.nativeId)
        : isSet(object.native_id)
        ? globalThis.String(object.native_id)
        : "",
      fullId: isSet(object.fullId)
        ? globalThis.String(object.fullId)
        : isSet(object.full_id)
        ? globalThis.String(object.full_id)
        : "",
    };
  },

  toJSON(message: UniversalMarketId): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.nativeId !== "") {
      obj.nativeId = message.nativeId;
    }
    if (message.fullId !== "") {
      obj.fullId = message.fullId;
    }
    return obj;
  },

  create(base?: DeepPartial<UniversalMarketId>): UniversalMarketId {
    return UniversalMarketId.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UniversalMarketId>): UniversalMarketId {
    const message = createBaseUniversalMarketId();
    message.provider = object.provider ?? "";
    message.nativeId = object.nativeId ?? "";
    message.fullId = object.fullId ?? "";
    return message;
  },
};

function createBaseProviderId(): ProviderId {
  return { domain: "", name: "" };
}

export const ProviderId: MessageFns<ProviderId> = {
  encode(message: ProviderId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderId {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: ProviderId): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<ProviderId>): ProviderId {
    return ProviderId.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProviderId>): ProviderId {
    const message = createBaseProviderId();
    message.domain = object.domain ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
