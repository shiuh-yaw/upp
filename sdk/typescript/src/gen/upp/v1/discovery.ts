// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: upp/v1/discovery.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "./common";
import {
  MarketType,
  marketTypeFromJSON,
  marketTypeToJSON,
  OrderType,
  orderTypeFromJSON,
  orderTypeToJSON,
  TimeInForce,
  timeInForceFromJSON,
  timeInForceToJSON,
} from "./market";
import { OracleType, oracleTypeFromJSON, oracleTypeToJSON } from "./resolution";

export const protobufPackage = "upp.v1";

export enum ProviderType {
  PROVIDER_TYPE_UNSPECIFIED = 0,
  /** PROVIDER_TYPE_REGULATED_EXCHANGE - CFTC/SEC regulated (Kalshi) */
  PROVIDER_TYPE_REGULATED_EXCHANGE = 1,
  /** PROVIDER_TYPE_DECENTRALIZED - On-chain, decentralized (Polymarket) */
  PROVIDER_TYPE_DECENTRALIZED = 2,
  /** PROVIDER_TYPE_HYBRID - Off-chain matching, on-chain settlement */
  PROVIDER_TYPE_HYBRID = 3,
  /** PROVIDER_TYPE_AGGREGATOR - Aggregates multiple providers (Dome) */
  PROVIDER_TYPE_AGGREGATOR = 4,
  UNRECOGNIZED = -1,
}

export function providerTypeFromJSON(object: any): ProviderType {
  switch (object) {
    case 0:
    case "PROVIDER_TYPE_UNSPECIFIED":
      return ProviderType.PROVIDER_TYPE_UNSPECIFIED;
    case 1:
    case "PROVIDER_TYPE_REGULATED_EXCHANGE":
      return ProviderType.PROVIDER_TYPE_REGULATED_EXCHANGE;
    case 2:
    case "PROVIDER_TYPE_DECENTRALIZED":
      return ProviderType.PROVIDER_TYPE_DECENTRALIZED;
    case 3:
    case "PROVIDER_TYPE_HYBRID":
      return ProviderType.PROVIDER_TYPE_HYBRID;
    case 4:
    case "PROVIDER_TYPE_AGGREGATOR":
      return ProviderType.PROVIDER_TYPE_AGGREGATOR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProviderType.UNRECOGNIZED;
  }
}

export function providerTypeToJSON(object: ProviderType): string {
  switch (object) {
    case ProviderType.PROVIDER_TYPE_UNSPECIFIED:
      return "PROVIDER_TYPE_UNSPECIFIED";
    case ProviderType.PROVIDER_TYPE_REGULATED_EXCHANGE:
      return "PROVIDER_TYPE_REGULATED_EXCHANGE";
    case ProviderType.PROVIDER_TYPE_DECENTRALIZED:
      return "PROVIDER_TYPE_DECENTRALIZED";
    case ProviderType.PROVIDER_TYPE_HYBRID:
      return "PROVIDER_TYPE_HYBRID";
    case ProviderType.PROVIDER_TYPE_AGGREGATOR:
      return "PROVIDER_TYPE_AGGREGATOR";
    case ProviderType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AuthMethodType {
  AUTH_METHOD_TYPE_UNSPECIFIED = 0,
  /** AUTH_METHOD_TYPE_API_KEY - Simple API key */
  AUTH_METHOD_TYPE_API_KEY = 1,
  /** AUTH_METHOD_TYPE_API_KEY_RSA - API key + RSA signature (Kalshi) */
  AUTH_METHOD_TYPE_API_KEY_RSA = 2,
  /** AUTH_METHOD_TYPE_WALLET_SIG - Ethereum wallet signature / EIP-712 (Polymarket) */
  AUTH_METHOD_TYPE_WALLET_SIG = 3,
  /** AUTH_METHOD_TYPE_OAUTH2 - OAuth 2.0 / OIDC */
  AUTH_METHOD_TYPE_OAUTH2 = 4,
  /** AUTH_METHOD_TYPE_MCP_TOKEN - MCP-specific token */
  AUTH_METHOD_TYPE_MCP_TOKEN = 5,
  /** AUTH_METHOD_TYPE_JWT - JWT Bearer token */
  AUTH_METHOD_TYPE_JWT = 6,
  UNRECOGNIZED = -1,
}

export function authMethodTypeFromJSON(object: any): AuthMethodType {
  switch (object) {
    case 0:
    case "AUTH_METHOD_TYPE_UNSPECIFIED":
      return AuthMethodType.AUTH_METHOD_TYPE_UNSPECIFIED;
    case 1:
    case "AUTH_METHOD_TYPE_API_KEY":
      return AuthMethodType.AUTH_METHOD_TYPE_API_KEY;
    case 2:
    case "AUTH_METHOD_TYPE_API_KEY_RSA":
      return AuthMethodType.AUTH_METHOD_TYPE_API_KEY_RSA;
    case 3:
    case "AUTH_METHOD_TYPE_WALLET_SIG":
      return AuthMethodType.AUTH_METHOD_TYPE_WALLET_SIG;
    case 4:
    case "AUTH_METHOD_TYPE_OAUTH2":
      return AuthMethodType.AUTH_METHOD_TYPE_OAUTH2;
    case 5:
    case "AUTH_METHOD_TYPE_MCP_TOKEN":
      return AuthMethodType.AUTH_METHOD_TYPE_MCP_TOKEN;
    case 6:
    case "AUTH_METHOD_TYPE_JWT":
      return AuthMethodType.AUTH_METHOD_TYPE_JWT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthMethodType.UNRECOGNIZED;
  }
}

export function authMethodTypeToJSON(object: AuthMethodType): string {
  switch (object) {
    case AuthMethodType.AUTH_METHOD_TYPE_UNSPECIFIED:
      return "AUTH_METHOD_TYPE_UNSPECIFIED";
    case AuthMethodType.AUTH_METHOD_TYPE_API_KEY:
      return "AUTH_METHOD_TYPE_API_KEY";
    case AuthMethodType.AUTH_METHOD_TYPE_API_KEY_RSA:
      return "AUTH_METHOD_TYPE_API_KEY_RSA";
    case AuthMethodType.AUTH_METHOD_TYPE_WALLET_SIG:
      return "AUTH_METHOD_TYPE_WALLET_SIG";
    case AuthMethodType.AUTH_METHOD_TYPE_OAUTH2:
      return "AUTH_METHOD_TYPE_OAUTH2";
    case AuthMethodType.AUTH_METHOD_TYPE_MCP_TOKEN:
      return "AUTH_METHOD_TYPE_MCP_TOKEN";
    case AuthMethodType.AUTH_METHOD_TYPE_JWT:
      return "AUTH_METHOD_TYPE_JWT";
    case AuthMethodType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Published at /.well-known/upp on the provider's domain.
 * This is the entry point for all UPP interactions.
 */
export interface ProviderManifest {
  /** Protocol version (date-based: "2026-03-11") */
  uppVersion: string;
  /** Provider identity */
  provider:
    | ProviderInfo
    | undefined;
  /** Supported capabilities */
  capabilities: CapabilityDeclaration[];
  /** Transport bindings */
  transport:
    | TransportConfig
    | undefined;
  /** Authentication methods */
  authentication:
    | AuthConfig
    | undefined;
  /** Rate limiting info */
  rateLimits:
    | RateLimitConfig
    | undefined;
  /** When this manifest was last updated */
  updatedAt: Timestamp | undefined;
}

export interface ProviderInfo {
  /** Human-readable name, e.g. "Kalshi" */
  name: string;
  /** Domain identifier, e.g. "kalshi.com" */
  id: string;
  /** Type of provider */
  type: ProviderType;
  /** Brief description */
  description: string;
  /** Provider website */
  websiteUrl: string;
  /** API documentation URL */
  docsUrl: string;
  /** Developer support contact */
  supportEmail: string;
  /** Provider logo */
  logoUrl: string;
  /** Regulatory info */
  regulatory: RegulatoryInfo | undefined;
}

export interface RegulatoryInfo {
  /** Where regulated */
  jurisdictions: string[];
  /** Regulatory body */
  regulator: string;
  /** License type */
  licenseType: string;
  /** Self-attestation */
  compliant: boolean;
  /** Compliance documentation */
  complianceUrl: string;
}

/**
 * A declared capability with version, operations, and extensions.
 * Mirroring UCP's capability model.
 */
export interface CapabilityDeclaration {
  /** "markets", "trading", "portfolio", etc. */
  name: string;
  /** Capability version (date-based) */
  version: string;
  /** URL to the capability's JSON Schema */
  schemaUrl: string;
  /** Supported operations within this capability */
  operations: string[];
  /** Active extensions for this capability */
  extensions: ExtensionDeclaration[];
  /** Capability-specific config */
  config: CapabilityConfig | undefined;
}

/**
 * An extension that augments a capability.
 * Mirroring UCP's extension model.
 */
export interface ExtensionDeclaration {
  /** "analytics", "streaming", "liquidity", etc. */
  name: string;
  version: string;
  /** URL to the extension's JSON Schema */
  schemaUrl: string;
  /** Operations added by this extension */
  operations: string[];
}

/** Capability-specific configuration. */
export interface CapabilityConfig {
  /** Markets capability config */
  supportedMarketTypes: MarketType[];
  /** Available categories */
  categories: string[];
  /** Trading capability config */
  supportedOrderTypes: OrderType[];
  supportedTif: TimeInForce[];
  /** Resolution capability config */
  supportedOracleTypes: OracleType[];
  disputeWindowHours: number;
  /** Settlement capability config */
  supportedCurrencies: string[];
  supportedFundingTypes: string[];
}

/** Transport configuration for a provider. */
export interface TransportConfig {
  rest: RestConfig | undefined;
  websocket: WebSocketConfig | undefined;
  grpc: GrpcConfig | undefined;
  mcp: McpConfig | undefined;
  a2a: A2aConfig | undefined;
  fix: FixConfig | undefined;
}

export interface RestConfig {
  /** e.g. "https://api.kalshi.com/upp/v1" */
  baseUrl: string;
  /** URL to OpenAPI 3.1 spec */
  openapiSpecUrl: string;
}

export interface WebSocketConfig {
  /** e.g. "wss://api.kalshi.com/upp/ws/v1" */
  url: string;
  /** Available subscription channels */
  channels: string[];
  heartbeatIntervalSeconds: number;
}

export interface GrpcConfig {
  /** e.g. "api.kalshi.com:443" */
  endpoint: string;
  tlsRequired: boolean;
  /** URL to .proto files */
  protoUrl: string;
}

export interface McpConfig {
  supported: boolean;
  /** URL to MCP manifest */
  manifestUrl: string;
  /** Available MCP tool names */
  tools: string[];
}

export interface A2aConfig {
  supported: boolean;
  /** URL to A2A agent card */
  agentCardUrl: string;
}

export interface FixConfig {
  supported: boolean;
  /** e.g. "4.4" */
  version: string;
  endpoint: string;
}

export interface AuthConfig {
  /** Supported auth methods */
  methods: AuthMethod[];
  /** Credential providers (third-party identity verification) */
  credentialProviders: CredentialProviderInfo[];
}

export interface AuthMethod {
  type: AuthMethodType;
  description: string;
  /** Documentation for this auth method */
  docsUrl: string;
}

export interface CredentialProviderInfo {
  /** e.g. "kalshi_native", "civic", "worldcoin" */
  id: string;
  /** Human-readable name */
  name: string;
  /** "native", "third_party" */
  type: string;
  docsUrl: string;
}

export interface RateLimitConfig {
  /** Per-endpoint rate limits */
  rules: RateLimitRule[];
  /** Tier info */
  tier: string;
  /** URL to request higher limits */
  upgradeUrl: string;
}

export interface RateLimitRule {
  /** Glob pattern, e.g. "/markets/*" */
  endpointPattern: string;
  requestsPerSecond: number;
  requestsPerMinute: number;
  /** Max burst above sustained rate */
  burstLimit: number;
}

/**
 * Player surface profile — declares what the surface supports.
 * Sent during the negotiation handshake.
 */
export interface SurfaceProfile {
  /** Surface identifier */
  id: string;
  /** Human-readable name */
  name: string;
  /** "trading_app", "ai_agent", "dashboard", etc. */
  type: string;
  /** What the surface supports */
  requestedCapabilities: string[];
  requestedExtensions: string[];
  preferredTransports: string[];
  supportedAuth: AuthMethodType[];
  /** Jurisdiction */
  jurisdiction: string;
}

/** Result of capability negotiation. */
export interface NegotiationResult {
  /** Intersection of surface and provider capabilities */
  activeCapabilities: CapabilityDeclaration[];
  activeExtensions: ExtensionDeclaration[];
  /** Selected transport */
  selectedTransport: string;
  /** Selected auth method */
  selectedAuth: AuthMethodType;
  /** Any restrictions */
  restrictions: string[];
}

export interface GetManifestRequest {
  /** Provider domain */
  provider: string;
}

export interface ListProvidersRequest {
  /** Optional: filter by jurisdiction */
  jurisdiction: string;
  /** Optional: filter by capability name */
  capability: string;
}

export interface ListProvidersResponse {
  providers: ProviderManifest[];
}

export interface NegotiateRequest {
  provider: string;
  surface: SurfaceProfile | undefined;
}

export interface HealthCheckRequest {
  provider: string;
}

export interface HealthCheckResponse {
  provider: string;
  healthy: boolean;
  /** "operational", "degraded", "down" */
  status: string;
  /** Current latency to provider */
  latencyMs: number;
  checkedAt: Timestamp | undefined;
}

function createBaseProviderManifest(): ProviderManifest {
  return {
    uppVersion: "",
    provider: undefined,
    capabilities: [],
    transport: undefined,
    authentication: undefined,
    rateLimits: undefined,
    updatedAt: undefined,
  };
}

export const ProviderManifest: MessageFns<ProviderManifest> = {
  encode(message: ProviderManifest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uppVersion !== "") {
      writer.uint32(10).string(message.uppVersion);
    }
    if (message.provider !== undefined) {
      ProviderInfo.encode(message.provider, writer.uint32(18).fork()).join();
    }
    for (const v of message.capabilities) {
      CapabilityDeclaration.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.transport !== undefined) {
      TransportConfig.encode(message.transport, writer.uint32(34).fork()).join();
    }
    if (message.authentication !== undefined) {
      AuthConfig.encode(message.authentication, writer.uint32(42).fork()).join();
    }
    if (message.rateLimits !== undefined) {
      RateLimitConfig.encode(message.rateLimits, writer.uint32(50).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(message.updatedAt, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderManifest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderManifest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uppVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = ProviderInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.capabilities.push(CapabilityDeclaration.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.transport = TransportConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.authentication = AuthConfig.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.rateLimits = RateLimitConfig.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updatedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderManifest {
    return {
      uppVersion: isSet(object.uppVersion)
        ? globalThis.String(object.uppVersion)
        : isSet(object.upp_version)
        ? globalThis.String(object.upp_version)
        : "",
      provider: isSet(object.provider) ? ProviderInfo.fromJSON(object.provider) : undefined,
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => CapabilityDeclaration.fromJSON(e))
        : [],
      transport: isSet(object.transport) ? TransportConfig.fromJSON(object.transport) : undefined,
      authentication: isSet(object.authentication) ? AuthConfig.fromJSON(object.authentication) : undefined,
      rateLimits: isSet(object.rateLimits)
        ? RateLimitConfig.fromJSON(object.rateLimits)
        : isSet(object.rate_limits)
        ? RateLimitConfig.fromJSON(object.rate_limits)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? Timestamp.fromJSON(object.updatedAt)
        : isSet(object.updated_at)
        ? Timestamp.fromJSON(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: ProviderManifest): unknown {
    const obj: any = {};
    if (message.uppVersion !== "") {
      obj.uppVersion = message.uppVersion;
    }
    if (message.provider !== undefined) {
      obj.provider = ProviderInfo.toJSON(message.provider);
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => CapabilityDeclaration.toJSON(e));
    }
    if (message.transport !== undefined) {
      obj.transport = TransportConfig.toJSON(message.transport);
    }
    if (message.authentication !== undefined) {
      obj.authentication = AuthConfig.toJSON(message.authentication);
    }
    if (message.rateLimits !== undefined) {
      obj.rateLimits = RateLimitConfig.toJSON(message.rateLimits);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = Timestamp.toJSON(message.updatedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<ProviderManifest>): ProviderManifest {
    return ProviderManifest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProviderManifest>): ProviderManifest {
    const message = createBaseProviderManifest();
    message.uppVersion = object.uppVersion ?? "";
    message.provider = (object.provider !== undefined && object.provider !== null)
      ? ProviderInfo.fromPartial(object.provider)
      : undefined;
    message.capabilities = object.capabilities?.map((e) => CapabilityDeclaration.fromPartial(e)) || [];
    message.transport = (object.transport !== undefined && object.transport !== null)
      ? TransportConfig.fromPartial(object.transport)
      : undefined;
    message.authentication = (object.authentication !== undefined && object.authentication !== null)
      ? AuthConfig.fromPartial(object.authentication)
      : undefined;
    message.rateLimits = (object.rateLimits !== undefined && object.rateLimits !== null)
      ? RateLimitConfig.fromPartial(object.rateLimits)
      : undefined;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null)
      ? Timestamp.fromPartial(object.updatedAt)
      : undefined;
    return message;
  },
};

function createBaseProviderInfo(): ProviderInfo {
  return {
    name: "",
    id: "",
    type: 0,
    description: "",
    websiteUrl: "",
    docsUrl: "",
    supportEmail: "",
    logoUrl: "",
    regulatory: undefined,
  };
}

export const ProviderInfo: MessageFns<ProviderInfo> = {
  encode(message: ProviderInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.websiteUrl !== "") {
      writer.uint32(42).string(message.websiteUrl);
    }
    if (message.docsUrl !== "") {
      writer.uint32(50).string(message.docsUrl);
    }
    if (message.supportEmail !== "") {
      writer.uint32(58).string(message.supportEmail);
    }
    if (message.logoUrl !== "") {
      writer.uint32(66).string(message.logoUrl);
    }
    if (message.regulatory !== undefined) {
      RegulatoryInfo.encode(message.regulatory, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.websiteUrl = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.docsUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.supportEmail = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.logoUrl = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.regulatory = RegulatoryInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? providerTypeFromJSON(object.type) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      websiteUrl: isSet(object.websiteUrl)
        ? globalThis.String(object.websiteUrl)
        : isSet(object.website_url)
        ? globalThis.String(object.website_url)
        : "",
      docsUrl: isSet(object.docsUrl)
        ? globalThis.String(object.docsUrl)
        : isSet(object.docs_url)
        ? globalThis.String(object.docs_url)
        : "",
      supportEmail: isSet(object.supportEmail)
        ? globalThis.String(object.supportEmail)
        : isSet(object.support_email)
        ? globalThis.String(object.support_email)
        : "",
      logoUrl: isSet(object.logoUrl)
        ? globalThis.String(object.logoUrl)
        : isSet(object.logo_url)
        ? globalThis.String(object.logo_url)
        : "",
      regulatory: isSet(object.regulatory) ? RegulatoryInfo.fromJSON(object.regulatory) : undefined,
    };
  },

  toJSON(message: ProviderInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== 0) {
      obj.type = providerTypeToJSON(message.type);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.websiteUrl !== "") {
      obj.websiteUrl = message.websiteUrl;
    }
    if (message.docsUrl !== "") {
      obj.docsUrl = message.docsUrl;
    }
    if (message.supportEmail !== "") {
      obj.supportEmail = message.supportEmail;
    }
    if (message.logoUrl !== "") {
      obj.logoUrl = message.logoUrl;
    }
    if (message.regulatory !== undefined) {
      obj.regulatory = RegulatoryInfo.toJSON(message.regulatory);
    }
    return obj;
  },

  create(base?: DeepPartial<ProviderInfo>): ProviderInfo {
    return ProviderInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProviderInfo>): ProviderInfo {
    const message = createBaseProviderInfo();
    message.name = object.name ?? "";
    message.id = object.id ?? "";
    message.type = object.type ?? 0;
    message.description = object.description ?? "";
    message.websiteUrl = object.websiteUrl ?? "";
    message.docsUrl = object.docsUrl ?? "";
    message.supportEmail = object.supportEmail ?? "";
    message.logoUrl = object.logoUrl ?? "";
    message.regulatory = (object.regulatory !== undefined && object.regulatory !== null)
      ? RegulatoryInfo.fromPartial(object.regulatory)
      : undefined;
    return message;
  },
};

function createBaseRegulatoryInfo(): RegulatoryInfo {
  return { jurisdictions: [], regulator: "", licenseType: "", compliant: false, complianceUrl: "" };
}

export const RegulatoryInfo: MessageFns<RegulatoryInfo> = {
  encode(message: RegulatoryInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.jurisdictions) {
      writer.uint32(10).string(v!);
    }
    if (message.regulator !== "") {
      writer.uint32(18).string(message.regulator);
    }
    if (message.licenseType !== "") {
      writer.uint32(26).string(message.licenseType);
    }
    if (message.compliant !== false) {
      writer.uint32(32).bool(message.compliant);
    }
    if (message.complianceUrl !== "") {
      writer.uint32(42).string(message.complianceUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegulatoryInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegulatoryInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jurisdictions.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.regulator = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.licenseType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.compliant = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.complianceUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegulatoryInfo {
    return {
      jurisdictions: globalThis.Array.isArray(object?.jurisdictions)
        ? object.jurisdictions.map((e: any) => globalThis.String(e))
        : [],
      regulator: isSet(object.regulator) ? globalThis.String(object.regulator) : "",
      licenseType: isSet(object.licenseType)
        ? globalThis.String(object.licenseType)
        : isSet(object.license_type)
        ? globalThis.String(object.license_type)
        : "",
      compliant: isSet(object.compliant) ? globalThis.Boolean(object.compliant) : false,
      complianceUrl: isSet(object.complianceUrl)
        ? globalThis.String(object.complianceUrl)
        : isSet(object.compliance_url)
        ? globalThis.String(object.compliance_url)
        : "",
    };
  },

  toJSON(message: RegulatoryInfo): unknown {
    const obj: any = {};
    if (message.jurisdictions?.length) {
      obj.jurisdictions = message.jurisdictions;
    }
    if (message.regulator !== "") {
      obj.regulator = message.regulator;
    }
    if (message.licenseType !== "") {
      obj.licenseType = message.licenseType;
    }
    if (message.compliant !== false) {
      obj.compliant = message.compliant;
    }
    if (message.complianceUrl !== "") {
      obj.complianceUrl = message.complianceUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<RegulatoryInfo>): RegulatoryInfo {
    return RegulatoryInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegulatoryInfo>): RegulatoryInfo {
    const message = createBaseRegulatoryInfo();
    message.jurisdictions = object.jurisdictions?.map((e) => e) || [];
    message.regulator = object.regulator ?? "";
    message.licenseType = object.licenseType ?? "";
    message.compliant = object.compliant ?? false;
    message.complianceUrl = object.complianceUrl ?? "";
    return message;
  },
};

function createBaseCapabilityDeclaration(): CapabilityDeclaration {
  return { name: "", version: "", schemaUrl: "", operations: [], extensions: [], config: undefined };
}

export const CapabilityDeclaration: MessageFns<CapabilityDeclaration> = {
  encode(message: CapabilityDeclaration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.schemaUrl !== "") {
      writer.uint32(26).string(message.schemaUrl);
    }
    for (const v of message.operations) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.extensions) {
      ExtensionDeclaration.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.config !== undefined) {
      CapabilityConfig.encode(message.config, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityDeclaration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityDeclaration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.schemaUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.operations.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extensions.push(ExtensionDeclaration.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.config = CapabilityConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityDeclaration {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      schemaUrl: isSet(object.schemaUrl)
        ? globalThis.String(object.schemaUrl)
        : isSet(object.schema_url)
        ? globalThis.String(object.schema_url)
        : "",
      operations: globalThis.Array.isArray(object?.operations)
        ? object.operations.map((e: any) => globalThis.String(e))
        : [],
      extensions: globalThis.Array.isArray(object?.extensions)
        ? object.extensions.map((e: any) => ExtensionDeclaration.fromJSON(e))
        : [],
      config: isSet(object.config) ? CapabilityConfig.fromJSON(object.config) : undefined,
    };
  },

  toJSON(message: CapabilityDeclaration): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.schemaUrl !== "") {
      obj.schemaUrl = message.schemaUrl;
    }
    if (message.operations?.length) {
      obj.operations = message.operations;
    }
    if (message.extensions?.length) {
      obj.extensions = message.extensions.map((e) => ExtensionDeclaration.toJSON(e));
    }
    if (message.config !== undefined) {
      obj.config = CapabilityConfig.toJSON(message.config);
    }
    return obj;
  },

  create(base?: DeepPartial<CapabilityDeclaration>): CapabilityDeclaration {
    return CapabilityDeclaration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CapabilityDeclaration>): CapabilityDeclaration {
    const message = createBaseCapabilityDeclaration();
    message.name = object.name ?? "";
    message.version = object.version ?? "";
    message.schemaUrl = object.schemaUrl ?? "";
    message.operations = object.operations?.map((e) => e) || [];
    message.extensions = object.extensions?.map((e) => ExtensionDeclaration.fromPartial(e)) || [];
    message.config = (object.config !== undefined && object.config !== null)
      ? CapabilityConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseExtensionDeclaration(): ExtensionDeclaration {
  return { name: "", version: "", schemaUrl: "", operations: [] };
}

export const ExtensionDeclaration: MessageFns<ExtensionDeclaration> = {
  encode(message: ExtensionDeclaration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.schemaUrl !== "") {
      writer.uint32(26).string(message.schemaUrl);
    }
    for (const v of message.operations) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtensionDeclaration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtensionDeclaration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.schemaUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.operations.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExtensionDeclaration {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      schemaUrl: isSet(object.schemaUrl)
        ? globalThis.String(object.schemaUrl)
        : isSet(object.schema_url)
        ? globalThis.String(object.schema_url)
        : "",
      operations: globalThis.Array.isArray(object?.operations)
        ? object.operations.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ExtensionDeclaration): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.schemaUrl !== "") {
      obj.schemaUrl = message.schemaUrl;
    }
    if (message.operations?.length) {
      obj.operations = message.operations;
    }
    return obj;
  },

  create(base?: DeepPartial<ExtensionDeclaration>): ExtensionDeclaration {
    return ExtensionDeclaration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExtensionDeclaration>): ExtensionDeclaration {
    const message = createBaseExtensionDeclaration();
    message.name = object.name ?? "";
    message.version = object.version ?? "";
    message.schemaUrl = object.schemaUrl ?? "";
    message.operations = object.operations?.map((e) => e) || [];
    return message;
  },
};

function createBaseCapabilityConfig(): CapabilityConfig {
  return {
    supportedMarketTypes: [],
    categories: [],
    supportedOrderTypes: [],
    supportedTif: [],
    supportedOracleTypes: [],
    disputeWindowHours: 0,
    supportedCurrencies: [],
    supportedFundingTypes: [],
  };
}

export const CapabilityConfig: MessageFns<CapabilityConfig> = {
  encode(message: CapabilityConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.supportedMarketTypes) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.categories) {
      writer.uint32(18).string(v!);
    }
    writer.uint32(26).fork();
    for (const v of message.supportedOrderTypes) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.supportedTif) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(42).fork();
    for (const v of message.supportedOracleTypes) {
      writer.int32(v);
    }
    writer.join();
    if (message.disputeWindowHours !== 0) {
      writer.uint32(48).int32(message.disputeWindowHours);
    }
    for (const v of message.supportedCurrencies) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.supportedFundingTypes) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.supportedMarketTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedMarketTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.categories.push(reader.string());
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.supportedOrderTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedOrderTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.supportedTif.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedTif.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag === 40) {
            message.supportedOracleTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedOracleTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.disputeWindowHours = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.supportedCurrencies.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.supportedFundingTypes.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityConfig {
    return {
      supportedMarketTypes: globalThis.Array.isArray(object?.supportedMarketTypes)
        ? object.supportedMarketTypes.map((e: any) => marketTypeFromJSON(e))
        : globalThis.Array.isArray(object?.supported_market_types)
        ? object.supported_market_types.map((e: any) => marketTypeFromJSON(e))
        : [],
      categories: globalThis.Array.isArray(object?.categories)
        ? object.categories.map((e: any) => globalThis.String(e))
        : [],
      supportedOrderTypes: globalThis.Array.isArray(object?.supportedOrderTypes)
        ? object.supportedOrderTypes.map((e: any) => orderTypeFromJSON(e))
        : globalThis.Array.isArray(object?.supported_order_types)
        ? object.supported_order_types.map((e: any) => orderTypeFromJSON(e))
        : [],
      supportedTif: globalThis.Array.isArray(object?.supportedTif)
        ? object.supportedTif.map((e: any) => timeInForceFromJSON(e))
        : globalThis.Array.isArray(object?.supported_tif)
        ? object.supported_tif.map((e: any) => timeInForceFromJSON(e))
        : [],
      supportedOracleTypes: globalThis.Array.isArray(object?.supportedOracleTypes)
        ? object.supportedOracleTypes.map((e: any) => oracleTypeFromJSON(e))
        : globalThis.Array.isArray(object?.supported_oracle_types)
        ? object.supported_oracle_types.map((e: any) => oracleTypeFromJSON(e))
        : [],
      disputeWindowHours: isSet(object.disputeWindowHours)
        ? globalThis.Number(object.disputeWindowHours)
        : isSet(object.dispute_window_hours)
        ? globalThis.Number(object.dispute_window_hours)
        : 0,
      supportedCurrencies: globalThis.Array.isArray(object?.supportedCurrencies)
        ? object.supportedCurrencies.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_currencies)
        ? object.supported_currencies.map((e: any) => globalThis.String(e))
        : [],
      supportedFundingTypes: globalThis.Array.isArray(object?.supportedFundingTypes)
        ? object.supportedFundingTypes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_funding_types)
        ? object.supported_funding_types.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: CapabilityConfig): unknown {
    const obj: any = {};
    if (message.supportedMarketTypes?.length) {
      obj.supportedMarketTypes = message.supportedMarketTypes.map((e) => marketTypeToJSON(e));
    }
    if (message.categories?.length) {
      obj.categories = message.categories;
    }
    if (message.supportedOrderTypes?.length) {
      obj.supportedOrderTypes = message.supportedOrderTypes.map((e) => orderTypeToJSON(e));
    }
    if (message.supportedTif?.length) {
      obj.supportedTif = message.supportedTif.map((e) => timeInForceToJSON(e));
    }
    if (message.supportedOracleTypes?.length) {
      obj.supportedOracleTypes = message.supportedOracleTypes.map((e) => oracleTypeToJSON(e));
    }
    if (message.disputeWindowHours !== 0) {
      obj.disputeWindowHours = Math.round(message.disputeWindowHours);
    }
    if (message.supportedCurrencies?.length) {
      obj.supportedCurrencies = message.supportedCurrencies;
    }
    if (message.supportedFundingTypes?.length) {
      obj.supportedFundingTypes = message.supportedFundingTypes;
    }
    return obj;
  },

  create(base?: DeepPartial<CapabilityConfig>): CapabilityConfig {
    return CapabilityConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CapabilityConfig>): CapabilityConfig {
    const message = createBaseCapabilityConfig();
    message.supportedMarketTypes = object.supportedMarketTypes?.map((e) => e) || [];
    message.categories = object.categories?.map((e) => e) || [];
    message.supportedOrderTypes = object.supportedOrderTypes?.map((e) => e) || [];
    message.supportedTif = object.supportedTif?.map((e) => e) || [];
    message.supportedOracleTypes = object.supportedOracleTypes?.map((e) => e) || [];
    message.disputeWindowHours = object.disputeWindowHours ?? 0;
    message.supportedCurrencies = object.supportedCurrencies?.map((e) => e) || [];
    message.supportedFundingTypes = object.supportedFundingTypes?.map((e) => e) || [];
    return message;
  },
};

function createBaseTransportConfig(): TransportConfig {
  return { rest: undefined, websocket: undefined, grpc: undefined, mcp: undefined, a2a: undefined, fix: undefined };
}

export const TransportConfig: MessageFns<TransportConfig> = {
  encode(message: TransportConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rest !== undefined) {
      RestConfig.encode(message.rest, writer.uint32(10).fork()).join();
    }
    if (message.websocket !== undefined) {
      WebSocketConfig.encode(message.websocket, writer.uint32(18).fork()).join();
    }
    if (message.grpc !== undefined) {
      GrpcConfig.encode(message.grpc, writer.uint32(26).fork()).join();
    }
    if (message.mcp !== undefined) {
      McpConfig.encode(message.mcp, writer.uint32(34).fork()).join();
    }
    if (message.a2a !== undefined) {
      A2aConfig.encode(message.a2a, writer.uint32(42).fork()).join();
    }
    if (message.fix !== undefined) {
      FixConfig.encode(message.fix, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransportConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransportConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rest = RestConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.websocket = WebSocketConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.grpc = GrpcConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mcp = McpConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.a2a = A2aConfig.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.fix = FixConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransportConfig {
    return {
      rest: isSet(object.rest) ? RestConfig.fromJSON(object.rest) : undefined,
      websocket: isSet(object.websocket) ? WebSocketConfig.fromJSON(object.websocket) : undefined,
      grpc: isSet(object.grpc) ? GrpcConfig.fromJSON(object.grpc) : undefined,
      mcp: isSet(object.mcp) ? McpConfig.fromJSON(object.mcp) : undefined,
      a2a: isSet(object.a2a) ? A2aConfig.fromJSON(object.a2a) : undefined,
      fix: isSet(object.fix) ? FixConfig.fromJSON(object.fix) : undefined,
    };
  },

  toJSON(message: TransportConfig): unknown {
    const obj: any = {};
    if (message.rest !== undefined) {
      obj.rest = RestConfig.toJSON(message.rest);
    }
    if (message.websocket !== undefined) {
      obj.websocket = WebSocketConfig.toJSON(message.websocket);
    }
    if (message.grpc !== undefined) {
      obj.grpc = GrpcConfig.toJSON(message.grpc);
    }
    if (message.mcp !== undefined) {
      obj.mcp = McpConfig.toJSON(message.mcp);
    }
    if (message.a2a !== undefined) {
      obj.a2a = A2aConfig.toJSON(message.a2a);
    }
    if (message.fix !== undefined) {
      obj.fix = FixConfig.toJSON(message.fix);
    }
    return obj;
  },

  create(base?: DeepPartial<TransportConfig>): TransportConfig {
    return TransportConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TransportConfig>): TransportConfig {
    const message = createBaseTransportConfig();
    message.rest = (object.rest !== undefined && object.rest !== null)
      ? RestConfig.fromPartial(object.rest)
      : undefined;
    message.websocket = (object.websocket !== undefined && object.websocket !== null)
      ? WebSocketConfig.fromPartial(object.websocket)
      : undefined;
    message.grpc = (object.grpc !== undefined && object.grpc !== null)
      ? GrpcConfig.fromPartial(object.grpc)
      : undefined;
    message.mcp = (object.mcp !== undefined && object.mcp !== null) ? McpConfig.fromPartial(object.mcp) : undefined;
    message.a2a = (object.a2a !== undefined && object.a2a !== null) ? A2aConfig.fromPartial(object.a2a) : undefined;
    message.fix = (object.fix !== undefined && object.fix !== null) ? FixConfig.fromPartial(object.fix) : undefined;
    return message;
  },
};

function createBaseRestConfig(): RestConfig {
  return { baseUrl: "", openapiSpecUrl: "" };
}

export const RestConfig: MessageFns<RestConfig> = {
  encode(message: RestConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.baseUrl !== "") {
      writer.uint32(10).string(message.baseUrl);
    }
    if (message.openapiSpecUrl !== "") {
      writer.uint32(18).string(message.openapiSpecUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.baseUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.openapiSpecUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RestConfig {
    return {
      baseUrl: isSet(object.baseUrl)
        ? globalThis.String(object.baseUrl)
        : isSet(object.base_url)
        ? globalThis.String(object.base_url)
        : "",
      openapiSpecUrl: isSet(object.openapiSpecUrl)
        ? globalThis.String(object.openapiSpecUrl)
        : isSet(object.openapi_spec_url)
        ? globalThis.String(object.openapi_spec_url)
        : "",
    };
  },

  toJSON(message: RestConfig): unknown {
    const obj: any = {};
    if (message.baseUrl !== "") {
      obj.baseUrl = message.baseUrl;
    }
    if (message.openapiSpecUrl !== "") {
      obj.openapiSpecUrl = message.openapiSpecUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<RestConfig>): RestConfig {
    return RestConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RestConfig>): RestConfig {
    const message = createBaseRestConfig();
    message.baseUrl = object.baseUrl ?? "";
    message.openapiSpecUrl = object.openapiSpecUrl ?? "";
    return message;
  },
};

function createBaseWebSocketConfig(): WebSocketConfig {
  return { url: "", channels: [], heartbeatIntervalSeconds: 0 };
}

export const WebSocketConfig: MessageFns<WebSocketConfig> = {
  encode(message: WebSocketConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    for (const v of message.channels) {
      writer.uint32(18).string(v!);
    }
    if (message.heartbeatIntervalSeconds !== 0) {
      writer.uint32(24).int32(message.heartbeatIntervalSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebSocketConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebSocketConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channels.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.heartbeatIntervalSeconds = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebSocketConfig {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      channels: globalThis.Array.isArray(object?.channels) ? object.channels.map((e: any) => globalThis.String(e)) : [],
      heartbeatIntervalSeconds: isSet(object.heartbeatIntervalSeconds)
        ? globalThis.Number(object.heartbeatIntervalSeconds)
        : isSet(object.heartbeat_interval_seconds)
        ? globalThis.Number(object.heartbeat_interval_seconds)
        : 0,
    };
  },

  toJSON(message: WebSocketConfig): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.channels?.length) {
      obj.channels = message.channels;
    }
    if (message.heartbeatIntervalSeconds !== 0) {
      obj.heartbeatIntervalSeconds = Math.round(message.heartbeatIntervalSeconds);
    }
    return obj;
  },

  create(base?: DeepPartial<WebSocketConfig>): WebSocketConfig {
    return WebSocketConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WebSocketConfig>): WebSocketConfig {
    const message = createBaseWebSocketConfig();
    message.url = object.url ?? "";
    message.channels = object.channels?.map((e) => e) || [];
    message.heartbeatIntervalSeconds = object.heartbeatIntervalSeconds ?? 0;
    return message;
  },
};

function createBaseGrpcConfig(): GrpcConfig {
  return { endpoint: "", tlsRequired: false, protoUrl: "" };
}

export const GrpcConfig: MessageFns<GrpcConfig> = {
  encode(message: GrpcConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpoint !== "") {
      writer.uint32(10).string(message.endpoint);
    }
    if (message.tlsRequired !== false) {
      writer.uint32(16).bool(message.tlsRequired);
    }
    if (message.protoUrl !== "") {
      writer.uint32(26).string(message.protoUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tlsRequired = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.protoUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcConfig {
    return {
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
      tlsRequired: isSet(object.tlsRequired)
        ? globalThis.Boolean(object.tlsRequired)
        : isSet(object.tls_required)
        ? globalThis.Boolean(object.tls_required)
        : false,
      protoUrl: isSet(object.protoUrl)
        ? globalThis.String(object.protoUrl)
        : isSet(object.proto_url)
        ? globalThis.String(object.proto_url)
        : "",
    };
  },

  toJSON(message: GrpcConfig): unknown {
    const obj: any = {};
    if (message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    if (message.tlsRequired !== false) {
      obj.tlsRequired = message.tlsRequired;
    }
    if (message.protoUrl !== "") {
      obj.protoUrl = message.protoUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<GrpcConfig>): GrpcConfig {
    return GrpcConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GrpcConfig>): GrpcConfig {
    const message = createBaseGrpcConfig();
    message.endpoint = object.endpoint ?? "";
    message.tlsRequired = object.tlsRequired ?? false;
    message.protoUrl = object.protoUrl ?? "";
    return message;
  },
};

function createBaseMcpConfig(): McpConfig {
  return { supported: false, manifestUrl: "", tools: [] };
}

export const McpConfig: MessageFns<McpConfig> = {
  encode(message: McpConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported !== false) {
      writer.uint32(8).bool(message.supported);
    }
    if (message.manifestUrl !== "") {
      writer.uint32(18).string(message.manifestUrl);
    }
    for (const v of message.tools) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): McpConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMcpConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.supported = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifestUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tools.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): McpConfig {
    return {
      supported: isSet(object.supported) ? globalThis.Boolean(object.supported) : false,
      manifestUrl: isSet(object.manifestUrl)
        ? globalThis.String(object.manifestUrl)
        : isSet(object.manifest_url)
        ? globalThis.String(object.manifest_url)
        : "",
      tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: McpConfig): unknown {
    const obj: any = {};
    if (message.supported !== false) {
      obj.supported = message.supported;
    }
    if (message.manifestUrl !== "") {
      obj.manifestUrl = message.manifestUrl;
    }
    if (message.tools?.length) {
      obj.tools = message.tools;
    }
    return obj;
  },

  create(base?: DeepPartial<McpConfig>): McpConfig {
    return McpConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<McpConfig>): McpConfig {
    const message = createBaseMcpConfig();
    message.supported = object.supported ?? false;
    message.manifestUrl = object.manifestUrl ?? "";
    message.tools = object.tools?.map((e) => e) || [];
    return message;
  },
};

function createBaseA2aConfig(): A2aConfig {
  return { supported: false, agentCardUrl: "" };
}

export const A2aConfig: MessageFns<A2aConfig> = {
  encode(message: A2aConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported !== false) {
      writer.uint32(8).bool(message.supported);
    }
    if (message.agentCardUrl !== "") {
      writer.uint32(18).string(message.agentCardUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): A2aConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseA2aConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.supported = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentCardUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): A2aConfig {
    return {
      supported: isSet(object.supported) ? globalThis.Boolean(object.supported) : false,
      agentCardUrl: isSet(object.agentCardUrl)
        ? globalThis.String(object.agentCardUrl)
        : isSet(object.agent_card_url)
        ? globalThis.String(object.agent_card_url)
        : "",
    };
  },

  toJSON(message: A2aConfig): unknown {
    const obj: any = {};
    if (message.supported !== false) {
      obj.supported = message.supported;
    }
    if (message.agentCardUrl !== "") {
      obj.agentCardUrl = message.agentCardUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<A2aConfig>): A2aConfig {
    return A2aConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<A2aConfig>): A2aConfig {
    const message = createBaseA2aConfig();
    message.supported = object.supported ?? false;
    message.agentCardUrl = object.agentCardUrl ?? "";
    return message;
  },
};

function createBaseFixConfig(): FixConfig {
  return { supported: false, version: "", endpoint: "" };
}

export const FixConfig: MessageFns<FixConfig> = {
  encode(message: FixConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported !== false) {
      writer.uint32(8).bool(message.supported);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.endpoint !== "") {
      writer.uint32(26).string(message.endpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFixConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.supported = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FixConfig {
    return {
      supported: isSet(object.supported) ? globalThis.Boolean(object.supported) : false,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : "",
    };
  },

  toJSON(message: FixConfig): unknown {
    const obj: any = {};
    if (message.supported !== false) {
      obj.supported = message.supported;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.endpoint !== "") {
      obj.endpoint = message.endpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<FixConfig>): FixConfig {
    return FixConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FixConfig>): FixConfig {
    const message = createBaseFixConfig();
    message.supported = object.supported ?? false;
    message.version = object.version ?? "";
    message.endpoint = object.endpoint ?? "";
    return message;
  },
};

function createBaseAuthConfig(): AuthConfig {
  return { methods: [], credentialProviders: [] };
}

export const AuthConfig: MessageFns<AuthConfig> = {
  encode(message: AuthConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.methods) {
      AuthMethod.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.credentialProviders) {
      CredentialProviderInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.methods.push(AuthMethod.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentialProviders.push(CredentialProviderInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthConfig {
    return {
      methods: globalThis.Array.isArray(object?.methods) ? object.methods.map((e: any) => AuthMethod.fromJSON(e)) : [],
      credentialProviders: globalThis.Array.isArray(object?.credentialProviders)
        ? object.credentialProviders.map((e: any) => CredentialProviderInfo.fromJSON(e))
        : globalThis.Array.isArray(object?.credential_providers)
        ? object.credential_providers.map((e: any) => CredentialProviderInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AuthConfig): unknown {
    const obj: any = {};
    if (message.methods?.length) {
      obj.methods = message.methods.map((e) => AuthMethod.toJSON(e));
    }
    if (message.credentialProviders?.length) {
      obj.credentialProviders = message.credentialProviders.map((e) => CredentialProviderInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AuthConfig>): AuthConfig {
    return AuthConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthConfig>): AuthConfig {
    const message = createBaseAuthConfig();
    message.methods = object.methods?.map((e) => AuthMethod.fromPartial(e)) || [];
    message.credentialProviders = object.credentialProviders?.map((e) => CredentialProviderInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAuthMethod(): AuthMethod {
  return { type: 0, description: "", docsUrl: "" };
}

export const AuthMethod: MessageFns<AuthMethod> = {
  encode(message: AuthMethod, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.docsUrl !== "") {
      writer.uint32(26).string(message.docsUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthMethod {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthMethod();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.docsUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthMethod {
    return {
      type: isSet(object.type) ? authMethodTypeFromJSON(object.type) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      docsUrl: isSet(object.docsUrl)
        ? globalThis.String(object.docsUrl)
        : isSet(object.docs_url)
        ? globalThis.String(object.docs_url)
        : "",
    };
  },

  toJSON(message: AuthMethod): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = authMethodTypeToJSON(message.type);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.docsUrl !== "") {
      obj.docsUrl = message.docsUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthMethod>): AuthMethod {
    return AuthMethod.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthMethod>): AuthMethod {
    const message = createBaseAuthMethod();
    message.type = object.type ?? 0;
    message.description = object.description ?? "";
    message.docsUrl = object.docsUrl ?? "";
    return message;
  },
};

function createBaseCredentialProviderInfo(): CredentialProviderInfo {
  return { id: "", name: "", type: "", docsUrl: "" };
}

export const CredentialProviderInfo: MessageFns<CredentialProviderInfo> = {
  encode(message: CredentialProviderInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.docsUrl !== "") {
      writer.uint32(34).string(message.docsUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CredentialProviderInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCredentialProviderInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.docsUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CredentialProviderInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      docsUrl: isSet(object.docsUrl)
        ? globalThis.String(object.docsUrl)
        : isSet(object.docs_url)
        ? globalThis.String(object.docs_url)
        : "",
    };
  },

  toJSON(message: CredentialProviderInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.docsUrl !== "") {
      obj.docsUrl = message.docsUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<CredentialProviderInfo>): CredentialProviderInfo {
    return CredentialProviderInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CredentialProviderInfo>): CredentialProviderInfo {
    const message = createBaseCredentialProviderInfo();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    message.docsUrl = object.docsUrl ?? "";
    return message;
  },
};

function createBaseRateLimitConfig(): RateLimitConfig {
  return { rules: [], tier: "", upgradeUrl: "" };
}

export const RateLimitConfig: MessageFns<RateLimitConfig> = {
  encode(message: RateLimitConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rules) {
      RateLimitRule.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.tier !== "") {
      writer.uint32(18).string(message.tier);
    }
    if (message.upgradeUrl !== "") {
      writer.uint32(26).string(message.upgradeUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rules.push(RateLimitRule.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.upgradeUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitConfig {
    return {
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => RateLimitRule.fromJSON(e)) : [],
      tier: isSet(object.tier) ? globalThis.String(object.tier) : "",
      upgradeUrl: isSet(object.upgradeUrl)
        ? globalThis.String(object.upgradeUrl)
        : isSet(object.upgrade_url)
        ? globalThis.String(object.upgrade_url)
        : "",
    };
  },

  toJSON(message: RateLimitConfig): unknown {
    const obj: any = {};
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => RateLimitRule.toJSON(e));
    }
    if (message.tier !== "") {
      obj.tier = message.tier;
    }
    if (message.upgradeUrl !== "") {
      obj.upgradeUrl = message.upgradeUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<RateLimitConfig>): RateLimitConfig {
    return RateLimitConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RateLimitConfig>): RateLimitConfig {
    const message = createBaseRateLimitConfig();
    message.rules = object.rules?.map((e) => RateLimitRule.fromPartial(e)) || [];
    message.tier = object.tier ?? "";
    message.upgradeUrl = object.upgradeUrl ?? "";
    return message;
  },
};

function createBaseRateLimitRule(): RateLimitRule {
  return { endpointPattern: "", requestsPerSecond: 0, requestsPerMinute: 0, burstLimit: 0 };
}

export const RateLimitRule: MessageFns<RateLimitRule> = {
  encode(message: RateLimitRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpointPattern !== "") {
      writer.uint32(10).string(message.endpointPattern);
    }
    if (message.requestsPerSecond !== 0) {
      writer.uint32(16).int32(message.requestsPerSecond);
    }
    if (message.requestsPerMinute !== 0) {
      writer.uint32(24).int32(message.requestsPerMinute);
    }
    if (message.burstLimit !== 0) {
      writer.uint32(32).int32(message.burstLimit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpointPattern = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestsPerSecond = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.requestsPerMinute = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.burstLimit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitRule {
    return {
      endpointPattern: isSet(object.endpointPattern)
        ? globalThis.String(object.endpointPattern)
        : isSet(object.endpoint_pattern)
        ? globalThis.String(object.endpoint_pattern)
        : "",
      requestsPerSecond: isSet(object.requestsPerSecond)
        ? globalThis.Number(object.requestsPerSecond)
        : isSet(object.requests_per_second)
        ? globalThis.Number(object.requests_per_second)
        : 0,
      requestsPerMinute: isSet(object.requestsPerMinute)
        ? globalThis.Number(object.requestsPerMinute)
        : isSet(object.requests_per_minute)
        ? globalThis.Number(object.requests_per_minute)
        : 0,
      burstLimit: isSet(object.burstLimit)
        ? globalThis.Number(object.burstLimit)
        : isSet(object.burst_limit)
        ? globalThis.Number(object.burst_limit)
        : 0,
    };
  },

  toJSON(message: RateLimitRule): unknown {
    const obj: any = {};
    if (message.endpointPattern !== "") {
      obj.endpointPattern = message.endpointPattern;
    }
    if (message.requestsPerSecond !== 0) {
      obj.requestsPerSecond = Math.round(message.requestsPerSecond);
    }
    if (message.requestsPerMinute !== 0) {
      obj.requestsPerMinute = Math.round(message.requestsPerMinute);
    }
    if (message.burstLimit !== 0) {
      obj.burstLimit = Math.round(message.burstLimit);
    }
    return obj;
  },

  create(base?: DeepPartial<RateLimitRule>): RateLimitRule {
    return RateLimitRule.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RateLimitRule>): RateLimitRule {
    const message = createBaseRateLimitRule();
    message.endpointPattern = object.endpointPattern ?? "";
    message.requestsPerSecond = object.requestsPerSecond ?? 0;
    message.requestsPerMinute = object.requestsPerMinute ?? 0;
    message.burstLimit = object.burstLimit ?? 0;
    return message;
  },
};

function createBaseSurfaceProfile(): SurfaceProfile {
  return {
    id: "",
    name: "",
    type: "",
    requestedCapabilities: [],
    requestedExtensions: [],
    preferredTransports: [],
    supportedAuth: [],
    jurisdiction: "",
  };
}

export const SurfaceProfile: MessageFns<SurfaceProfile> = {
  encode(message: SurfaceProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    for (const v of message.requestedCapabilities) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.requestedExtensions) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.preferredTransports) {
      writer.uint32(50).string(v!);
    }
    writer.uint32(58).fork();
    for (const v of message.supportedAuth) {
      writer.int32(v);
    }
    writer.join();
    if (message.jurisdiction !== "") {
      writer.uint32(66).string(message.jurisdiction);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SurfaceProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSurfaceProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestedCapabilities.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.requestedExtensions.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.preferredTransports.push(reader.string());
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.supportedAuth.push(reader.int32() as any);

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedAuth.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SurfaceProfile {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      requestedCapabilities: globalThis.Array.isArray(object?.requestedCapabilities)
        ? object.requestedCapabilities.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.requested_capabilities)
        ? object.requested_capabilities.map((e: any) => globalThis.String(e))
        : [],
      requestedExtensions: globalThis.Array.isArray(object?.requestedExtensions)
        ? object.requestedExtensions.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.requested_extensions)
        ? object.requested_extensions.map((e: any) => globalThis.String(e))
        : [],
      preferredTransports: globalThis.Array.isArray(object?.preferredTransports)
        ? object.preferredTransports.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.preferred_transports)
        ? object.preferred_transports.map((e: any) => globalThis.String(e))
        : [],
      supportedAuth: globalThis.Array.isArray(object?.supportedAuth)
        ? object.supportedAuth.map((e: any) => authMethodTypeFromJSON(e))
        : globalThis.Array.isArray(object?.supported_auth)
        ? object.supported_auth.map((e: any) => authMethodTypeFromJSON(e))
        : [],
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
    };
  },

  toJSON(message: SurfaceProfile): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.requestedCapabilities?.length) {
      obj.requestedCapabilities = message.requestedCapabilities;
    }
    if (message.requestedExtensions?.length) {
      obj.requestedExtensions = message.requestedExtensions;
    }
    if (message.preferredTransports?.length) {
      obj.preferredTransports = message.preferredTransports;
    }
    if (message.supportedAuth?.length) {
      obj.supportedAuth = message.supportedAuth.map((e) => authMethodTypeToJSON(e));
    }
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    return obj;
  },

  create(base?: DeepPartial<SurfaceProfile>): SurfaceProfile {
    return SurfaceProfile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SurfaceProfile>): SurfaceProfile {
    const message = createBaseSurfaceProfile();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    message.requestedCapabilities = object.requestedCapabilities?.map((e) => e) || [];
    message.requestedExtensions = object.requestedExtensions?.map((e) => e) || [];
    message.preferredTransports = object.preferredTransports?.map((e) => e) || [];
    message.supportedAuth = object.supportedAuth?.map((e) => e) || [];
    message.jurisdiction = object.jurisdiction ?? "";
    return message;
  },
};

function createBaseNegotiationResult(): NegotiationResult {
  return { activeCapabilities: [], activeExtensions: [], selectedTransport: "", selectedAuth: 0, restrictions: [] };
}

export const NegotiationResult: MessageFns<NegotiationResult> = {
  encode(message: NegotiationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.activeCapabilities) {
      CapabilityDeclaration.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.activeExtensions) {
      ExtensionDeclaration.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.selectedTransport !== "") {
      writer.uint32(26).string(message.selectedTransport);
    }
    if (message.selectedAuth !== 0) {
      writer.uint32(32).int32(message.selectedAuth);
    }
    for (const v of message.restrictions) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NegotiationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNegotiationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activeCapabilities.push(CapabilityDeclaration.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.activeExtensions.push(ExtensionDeclaration.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.selectedTransport = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.selectedAuth = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.restrictions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NegotiationResult {
    return {
      activeCapabilities: globalThis.Array.isArray(object?.activeCapabilities)
        ? object.activeCapabilities.map((e: any) => CapabilityDeclaration.fromJSON(e))
        : globalThis.Array.isArray(object?.active_capabilities)
        ? object.active_capabilities.map((e: any) => CapabilityDeclaration.fromJSON(e))
        : [],
      activeExtensions: globalThis.Array.isArray(object?.activeExtensions)
        ? object.activeExtensions.map((e: any) => ExtensionDeclaration.fromJSON(e))
        : globalThis.Array.isArray(object?.active_extensions)
        ? object.active_extensions.map((e: any) => ExtensionDeclaration.fromJSON(e))
        : [],
      selectedTransport: isSet(object.selectedTransport)
        ? globalThis.String(object.selectedTransport)
        : isSet(object.selected_transport)
        ? globalThis.String(object.selected_transport)
        : "",
      selectedAuth: isSet(object.selectedAuth)
        ? authMethodTypeFromJSON(object.selectedAuth)
        : isSet(object.selected_auth)
        ? authMethodTypeFromJSON(object.selected_auth)
        : 0,
      restrictions: globalThis.Array.isArray(object?.restrictions)
        ? object.restrictions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: NegotiationResult): unknown {
    const obj: any = {};
    if (message.activeCapabilities?.length) {
      obj.activeCapabilities = message.activeCapabilities.map((e) => CapabilityDeclaration.toJSON(e));
    }
    if (message.activeExtensions?.length) {
      obj.activeExtensions = message.activeExtensions.map((e) => ExtensionDeclaration.toJSON(e));
    }
    if (message.selectedTransport !== "") {
      obj.selectedTransport = message.selectedTransport;
    }
    if (message.selectedAuth !== 0) {
      obj.selectedAuth = authMethodTypeToJSON(message.selectedAuth);
    }
    if (message.restrictions?.length) {
      obj.restrictions = message.restrictions;
    }
    return obj;
  },

  create(base?: DeepPartial<NegotiationResult>): NegotiationResult {
    return NegotiationResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NegotiationResult>): NegotiationResult {
    const message = createBaseNegotiationResult();
    message.activeCapabilities = object.activeCapabilities?.map((e) => CapabilityDeclaration.fromPartial(e)) || [];
    message.activeExtensions = object.activeExtensions?.map((e) => ExtensionDeclaration.fromPartial(e)) || [];
    message.selectedTransport = object.selectedTransport ?? "";
    message.selectedAuth = object.selectedAuth ?? 0;
    message.restrictions = object.restrictions?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetManifestRequest(): GetManifestRequest {
  return { provider: "" };
}

export const GetManifestRequest: MessageFns<GetManifestRequest> = {
  encode(message: GetManifestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetManifestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetManifestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetManifestRequest {
    return { provider: isSet(object.provider) ? globalThis.String(object.provider) : "" };
  },

  toJSON(message: GetManifestRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create(base?: DeepPartial<GetManifestRequest>): GetManifestRequest {
    return GetManifestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetManifestRequest>): GetManifestRequest {
    const message = createBaseGetManifestRequest();
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseListProvidersRequest(): ListProvidersRequest {
  return { jurisdiction: "", capability: "" };
}

export const ListProvidersRequest: MessageFns<ListProvidersRequest> = {
  encode(message: ListProvidersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jurisdiction !== "") {
      writer.uint32(10).string(message.jurisdiction);
    }
    if (message.capability !== "") {
      writer.uint32(18).string(message.capability);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProvidersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capability = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProvidersRequest {
    return {
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
    };
  },

  toJSON(message: ListProvidersRequest): unknown {
    const obj: any = {};
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    return obj;
  },

  create(base?: DeepPartial<ListProvidersRequest>): ListProvidersRequest {
    return ListProvidersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListProvidersRequest>): ListProvidersRequest {
    const message = createBaseListProvidersRequest();
    message.jurisdiction = object.jurisdiction ?? "";
    message.capability = object.capability ?? "";
    return message;
  },
};

function createBaseListProvidersResponse(): ListProvidersResponse {
  return { providers: [] };
}

export const ListProvidersResponse: MessageFns<ListProvidersResponse> = {
  encode(message: ListProvidersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.providers) {
      ProviderManifest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProvidersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providers.push(ProviderManifest.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProvidersResponse {
    return {
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => ProviderManifest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProvidersResponse): unknown {
    const obj: any = {};
    if (message.providers?.length) {
      obj.providers = message.providers.map((e) => ProviderManifest.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListProvidersResponse>): ListProvidersResponse {
    return ListProvidersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListProvidersResponse>): ListProvidersResponse {
    const message = createBaseListProvidersResponse();
    message.providers = object.providers?.map((e) => ProviderManifest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNegotiateRequest(): NegotiateRequest {
  return { provider: "", surface: undefined };
}

export const NegotiateRequest: MessageFns<NegotiateRequest> = {
  encode(message: NegotiateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.surface !== undefined) {
      SurfaceProfile.encode(message.surface, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NegotiateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNegotiateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.surface = SurfaceProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NegotiateRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      surface: isSet(object.surface) ? SurfaceProfile.fromJSON(object.surface) : undefined,
    };
  },

  toJSON(message: NegotiateRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.surface !== undefined) {
      obj.surface = SurfaceProfile.toJSON(message.surface);
    }
    return obj;
  },

  create(base?: DeepPartial<NegotiateRequest>): NegotiateRequest {
    return NegotiateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NegotiateRequest>): NegotiateRequest {
    const message = createBaseNegotiateRequest();
    message.provider = object.provider ?? "";
    message.surface = (object.surface !== undefined && object.surface !== null)
      ? SurfaceProfile.fromPartial(object.surface)
      : undefined;
    return message;
  },
};

function createBaseHealthCheckRequest(): HealthCheckRequest {
  return { provider: "" };
}

export const HealthCheckRequest: MessageFns<HealthCheckRequest> = {
  encode(message: HealthCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckRequest {
    return { provider: isSet(object.provider) ? globalThis.String(object.provider) : "" };
  },

  toJSON(message: HealthCheckRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthCheckRequest>): HealthCheckRequest {
    return HealthCheckRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthCheckRequest>): HealthCheckRequest {
    const message = createBaseHealthCheckRequest();
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseHealthCheckResponse(): HealthCheckResponse {
  return { provider: "", healthy: false, status: "", latencyMs: 0, checkedAt: undefined };
}

export const HealthCheckResponse: MessageFns<HealthCheckResponse> = {
  encode(message: HealthCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.healthy !== false) {
      writer.uint32(16).bool(message.healthy);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.latencyMs !== 0) {
      writer.uint32(32).int64(message.latencyMs);
    }
    if (message.checkedAt !== undefined) {
      Timestamp.encode(message.checkedAt, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.healthy = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.latencyMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.checkedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthCheckResponse {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      healthy: isSet(object.healthy) ? globalThis.Boolean(object.healthy) : false,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      latencyMs: isSet(object.latencyMs)
        ? globalThis.Number(object.latencyMs)
        : isSet(object.latency_ms)
        ? globalThis.Number(object.latency_ms)
        : 0,
      checkedAt: isSet(object.checkedAt)
        ? Timestamp.fromJSON(object.checkedAt)
        : isSet(object.checked_at)
        ? Timestamp.fromJSON(object.checked_at)
        : undefined,
    };
  },

  toJSON(message: HealthCheckResponse): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.healthy !== false) {
      obj.healthy = message.healthy;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.latencyMs !== 0) {
      obj.latencyMs = Math.round(message.latencyMs);
    }
    if (message.checkedAt !== undefined) {
      obj.checkedAt = Timestamp.toJSON(message.checkedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<HealthCheckResponse>): HealthCheckResponse {
    return HealthCheckResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthCheckResponse>): HealthCheckResponse {
    const message = createBaseHealthCheckResponse();
    message.provider = object.provider ?? "";
    message.healthy = object.healthy ?? false;
    message.status = object.status ?? "";
    message.latencyMs = object.latencyMs ?? 0;
    message.checkedAt = (object.checkedAt !== undefined && object.checkedAt !== null)
      ? Timestamp.fromPartial(object.checkedAt)
      : undefined;
    return message;
  },
};

export interface DiscoveryServiceImplementation<CallContextExt = {}> {
  /** Fetch a provider's manifest. */
  getManifest(
    request: GetManifestRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ProviderManifest>>;
  /** List all registered providers. */
  listProviders(
    request: ListProvidersRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListProvidersResponse>>;
  /** Negotiate capabilities between a surface and provider. */
  negotiate(request: NegotiateRequest, context: CallContext & CallContextExt): Promise<DeepPartial<NegotiationResult>>;
  /** Health check for a specific provider. */
  healthCheck(
    request: HealthCheckRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<HealthCheckResponse>>;
}

export interface DiscoveryServiceClient<CallOptionsExt = {}> {
  /** Fetch a provider's manifest. */
  getManifest(
    request: DeepPartial<GetManifestRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ProviderManifest>;
  /** List all registered providers. */
  listProviders(
    request: DeepPartial<ListProvidersRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListProvidersResponse>;
  /** Negotiate capabilities between a surface and provider. */
  negotiate(request: DeepPartial<NegotiateRequest>, options?: CallOptions & CallOptionsExt): Promise<NegotiationResult>;
  /** Health check for a specific provider. */
  healthCheck(
    request: DeepPartial<HealthCheckRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<HealthCheckResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
