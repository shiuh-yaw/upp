// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: upp/v1/market.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import {
  PaginationRequest,
  PaginationResponse,
  SortDirection,
  sortDirectionFromJSON,
  sortDirectionToJSON,
  Timestamp,
  UniversalMarketId,
} from "./common";

export const protobufPackage = "upp.v1";

export enum MarketType {
  MARKET_TYPE_UNSPECIFIED = 0,
  /** MARKET_TYPE_BINARY - Yes/No — most common (Kalshi, Polymarket) */
  MARKET_TYPE_BINARY = 1,
  /** MARKET_TYPE_CATEGORICAL - Multiple exclusive outcomes (Opinion) */
  MARKET_TYPE_CATEGORICAL = 2,
  /** MARKET_TYPE_SCALAR - Numeric range with continuous outcomes */
  MARKET_TYPE_SCALAR = 3,
  UNRECOGNIZED = -1,
}

export function marketTypeFromJSON(object: any): MarketType {
  switch (object) {
    case 0:
    case "MARKET_TYPE_UNSPECIFIED":
      return MarketType.MARKET_TYPE_UNSPECIFIED;
    case 1:
    case "MARKET_TYPE_BINARY":
      return MarketType.MARKET_TYPE_BINARY;
    case 2:
    case "MARKET_TYPE_CATEGORICAL":
      return MarketType.MARKET_TYPE_CATEGORICAL;
    case 3:
    case "MARKET_TYPE_SCALAR":
      return MarketType.MARKET_TYPE_SCALAR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MarketType.UNRECOGNIZED;
  }
}

export function marketTypeToJSON(object: MarketType): string {
  switch (object) {
    case MarketType.MARKET_TYPE_UNSPECIFIED:
      return "MARKET_TYPE_UNSPECIFIED";
    case MarketType.MARKET_TYPE_BINARY:
      return "MARKET_TYPE_BINARY";
    case MarketType.MARKET_TYPE_CATEGORICAL:
      return "MARKET_TYPE_CATEGORICAL";
    case MarketType.MARKET_TYPE_SCALAR:
      return "MARKET_TYPE_SCALAR";
    case MarketType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MarketStatus {
  MARKET_STATUS_UNSPECIFIED = 0,
  /** MARKET_STATUS_PENDING - Created but not yet open */
  MARKET_STATUS_PENDING = 1,
  /** MARKET_STATUS_OPEN - Actively trading */
  MARKET_STATUS_OPEN = 2,
  /** MARKET_STATUS_HALTED - Temporarily suspended */
  MARKET_STATUS_HALTED = 3,
  /** MARKET_STATUS_CLOSED - Trading closed, awaiting resolution */
  MARKET_STATUS_CLOSED = 4,
  /** MARKET_STATUS_RESOLVED - Outcome determined */
  MARKET_STATUS_RESOLVED = 5,
  /** MARKET_STATUS_DISPUTED - Resolution under dispute */
  MARKET_STATUS_DISPUTED = 6,
  /** MARKET_STATUS_VOIDED - Market cancelled/voided */
  MARKET_STATUS_VOIDED = 7,
  UNRECOGNIZED = -1,
}

export function marketStatusFromJSON(object: any): MarketStatus {
  switch (object) {
    case 0:
    case "MARKET_STATUS_UNSPECIFIED":
      return MarketStatus.MARKET_STATUS_UNSPECIFIED;
    case 1:
    case "MARKET_STATUS_PENDING":
      return MarketStatus.MARKET_STATUS_PENDING;
    case 2:
    case "MARKET_STATUS_OPEN":
      return MarketStatus.MARKET_STATUS_OPEN;
    case 3:
    case "MARKET_STATUS_HALTED":
      return MarketStatus.MARKET_STATUS_HALTED;
    case 4:
    case "MARKET_STATUS_CLOSED":
      return MarketStatus.MARKET_STATUS_CLOSED;
    case 5:
    case "MARKET_STATUS_RESOLVED":
      return MarketStatus.MARKET_STATUS_RESOLVED;
    case 6:
    case "MARKET_STATUS_DISPUTED":
      return MarketStatus.MARKET_STATUS_DISPUTED;
    case 7:
    case "MARKET_STATUS_VOIDED":
      return MarketStatus.MARKET_STATUS_VOIDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MarketStatus.UNRECOGNIZED;
  }
}

export function marketStatusToJSON(object: MarketStatus): string {
  switch (object) {
    case MarketStatus.MARKET_STATUS_UNSPECIFIED:
      return "MARKET_STATUS_UNSPECIFIED";
    case MarketStatus.MARKET_STATUS_PENDING:
      return "MARKET_STATUS_PENDING";
    case MarketStatus.MARKET_STATUS_OPEN:
      return "MARKET_STATUS_OPEN";
    case MarketStatus.MARKET_STATUS_HALTED:
      return "MARKET_STATUS_HALTED";
    case MarketStatus.MARKET_STATUS_CLOSED:
      return "MARKET_STATUS_CLOSED";
    case MarketStatus.MARKET_STATUS_RESOLVED:
      return "MARKET_STATUS_RESOLVED";
    case MarketStatus.MARKET_STATUS_DISPUTED:
      return "MARKET_STATUS_DISPUTED";
    case MarketStatus.MARKET_STATUS_VOIDED:
      return "MARKET_STATUS_VOIDED";
    case MarketStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Order types supported by the market. */
export enum OrderType {
  ORDER_TYPE_UNSPECIFIED = 0,
  /** ORDER_TYPE_LIMIT - Limit order at specified price */
  ORDER_TYPE_LIMIT = 1,
  /** ORDER_TYPE_MARKET - Market order at best available price */
  ORDER_TYPE_MARKET = 2,
  UNRECOGNIZED = -1,
}

export function orderTypeFromJSON(object: any): OrderType {
  switch (object) {
    case 0:
    case "ORDER_TYPE_UNSPECIFIED":
      return OrderType.ORDER_TYPE_UNSPECIFIED;
    case 1:
    case "ORDER_TYPE_LIMIT":
      return OrderType.ORDER_TYPE_LIMIT;
    case 2:
    case "ORDER_TYPE_MARKET":
      return OrderType.ORDER_TYPE_MARKET;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderType.UNRECOGNIZED;
  }
}

export function orderTypeToJSON(object: OrderType): string {
  switch (object) {
    case OrderType.ORDER_TYPE_UNSPECIFIED:
      return "ORDER_TYPE_UNSPECIFIED";
    case OrderType.ORDER_TYPE_LIMIT:
      return "ORDER_TYPE_LIMIT";
    case OrderType.ORDER_TYPE_MARKET:
      return "ORDER_TYPE_MARKET";
    case OrderType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Time-in-force policies. */
export enum TimeInForce {
  TIME_IN_FORCE_UNSPECIFIED = 0,
  /** TIME_IN_FORCE_GTC - Good 'Til Cancelled */
  TIME_IN_FORCE_GTC = 1,
  /** TIME_IN_FORCE_GTD - Good 'Til Date */
  TIME_IN_FORCE_GTD = 2,
  /** TIME_IN_FORCE_FOK - Fill or Kill */
  TIME_IN_FORCE_FOK = 3,
  /** TIME_IN_FORCE_IOC - Immediate or Cancel */
  TIME_IN_FORCE_IOC = 4,
  UNRECOGNIZED = -1,
}

export function timeInForceFromJSON(object: any): TimeInForce {
  switch (object) {
    case 0:
    case "TIME_IN_FORCE_UNSPECIFIED":
      return TimeInForce.TIME_IN_FORCE_UNSPECIFIED;
    case 1:
    case "TIME_IN_FORCE_GTC":
      return TimeInForce.TIME_IN_FORCE_GTC;
    case 2:
    case "TIME_IN_FORCE_GTD":
      return TimeInForce.TIME_IN_FORCE_GTD;
    case 3:
    case "TIME_IN_FORCE_FOK":
      return TimeInForce.TIME_IN_FORCE_FOK;
    case 4:
    case "TIME_IN_FORCE_IOC":
      return TimeInForce.TIME_IN_FORCE_IOC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TimeInForce.UNRECOGNIZED;
  }
}

export function timeInForceToJSON(object: TimeInForce): string {
  switch (object) {
    case TimeInForce.TIME_IN_FORCE_UNSPECIFIED:
      return "TIME_IN_FORCE_UNSPECIFIED";
    case TimeInForce.TIME_IN_FORCE_GTC:
      return "TIME_IN_FORCE_GTC";
    case TimeInForce.TIME_IN_FORCE_GTD:
      return "TIME_IN_FORCE_GTD";
    case TimeInForce.TIME_IN_FORCE_FOK:
      return "TIME_IN_FORCE_FOK";
    case TimeInForce.TIME_IN_FORCE_IOC:
      return "TIME_IN_FORCE_IOC";
    case TimeInForce.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum KycLevel {
  KYC_LEVEL_UNSPECIFIED = 0,
  /** KYC_LEVEL_NONE - No KYC required (decentralized) */
  KYC_LEVEL_NONE = 1,
  /** KYC_LEVEL_BASIC - Email + phone verification */
  KYC_LEVEL_BASIC = 2,
  /** KYC_LEVEL_ENHANCED - Government ID + address verification */
  KYC_LEVEL_ENHANCED = 3,
  /** KYC_LEVEL_INSTITUTIONAL - Full institutional onboarding */
  KYC_LEVEL_INSTITUTIONAL = 4,
  UNRECOGNIZED = -1,
}

export function kycLevelFromJSON(object: any): KycLevel {
  switch (object) {
    case 0:
    case "KYC_LEVEL_UNSPECIFIED":
      return KycLevel.KYC_LEVEL_UNSPECIFIED;
    case 1:
    case "KYC_LEVEL_NONE":
      return KycLevel.KYC_LEVEL_NONE;
    case 2:
    case "KYC_LEVEL_BASIC":
      return KycLevel.KYC_LEVEL_BASIC;
    case 3:
    case "KYC_LEVEL_ENHANCED":
      return KycLevel.KYC_LEVEL_ENHANCED;
    case 4:
    case "KYC_LEVEL_INSTITUTIONAL":
      return KycLevel.KYC_LEVEL_INSTITUTIONAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KycLevel.UNRECOGNIZED;
  }
}

export function kycLevelToJSON(object: KycLevel): string {
  switch (object) {
    case KycLevel.KYC_LEVEL_UNSPECIFIED:
      return "KYC_LEVEL_UNSPECIFIED";
    case KycLevel.KYC_LEVEL_NONE:
      return "KYC_LEVEL_NONE";
    case KycLevel.KYC_LEVEL_BASIC:
      return "KYC_LEVEL_BASIC";
    case KycLevel.KYC_LEVEL_ENHANCED:
      return "KYC_LEVEL_ENHANCED";
    case KycLevel.KYC_LEVEL_INSTITUTIONAL:
      return "KYC_LEVEL_INSTITUTIONAL";
    case KycLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MarketSortField {
  MARKET_SORT_FIELD_UNSPECIFIED = 0,
  MARKET_SORT_FIELD_VOLUME_24H = 1,
  MARKET_SORT_FIELD_CREATED_AT = 2,
  MARKET_SORT_FIELD_CLOSES_AT = 3,
  MARKET_SORT_FIELD_OPEN_INTEREST = 4,
  MARKET_SORT_FIELD_NUM_TRADERS = 5,
  UNRECOGNIZED = -1,
}

export function marketSortFieldFromJSON(object: any): MarketSortField {
  switch (object) {
    case 0:
    case "MARKET_SORT_FIELD_UNSPECIFIED":
      return MarketSortField.MARKET_SORT_FIELD_UNSPECIFIED;
    case 1:
    case "MARKET_SORT_FIELD_VOLUME_24H":
      return MarketSortField.MARKET_SORT_FIELD_VOLUME_24H;
    case 2:
    case "MARKET_SORT_FIELD_CREATED_AT":
      return MarketSortField.MARKET_SORT_FIELD_CREATED_AT;
    case 3:
    case "MARKET_SORT_FIELD_CLOSES_AT":
      return MarketSortField.MARKET_SORT_FIELD_CLOSES_AT;
    case 4:
    case "MARKET_SORT_FIELD_OPEN_INTEREST":
      return MarketSortField.MARKET_SORT_FIELD_OPEN_INTEREST;
    case 5:
    case "MARKET_SORT_FIELD_NUM_TRADERS":
      return MarketSortField.MARKET_SORT_FIELD_NUM_TRADERS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MarketSortField.UNRECOGNIZED;
  }
}

export function marketSortFieldToJSON(object: MarketSortField): string {
  switch (object) {
    case MarketSortField.MARKET_SORT_FIELD_UNSPECIFIED:
      return "MARKET_SORT_FIELD_UNSPECIFIED";
    case MarketSortField.MARKET_SORT_FIELD_VOLUME_24H:
      return "MARKET_SORT_FIELD_VOLUME_24H";
    case MarketSortField.MARKET_SORT_FIELD_CREATED_AT:
      return "MARKET_SORT_FIELD_CREATED_AT";
    case MarketSortField.MARKET_SORT_FIELD_CLOSES_AT:
      return "MARKET_SORT_FIELD_CLOSES_AT";
    case MarketSortField.MARKET_SORT_FIELD_OPEN_INTEREST:
      return "MARKET_SORT_FIELD_OPEN_INTEREST";
    case MarketSortField.MARKET_SORT_FIELD_NUM_TRADERS:
      return "MARKET_SORT_FIELD_NUM_TRADERS";
    case MarketSortField.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A prediction market — the core tradeable entity.
 * Equivalent to UCP's "Product" in commerce.
 */
export interface Market {
  /** Universal ID: upp:{provider}:{native_id} */
  id:
    | UniversalMarketId
    | undefined;
  /** The event being predicted */
  event:
    | Event
    | undefined;
  /** Binary, categorical, or scalar */
  marketType: MarketType;
  /** Possible outcomes (2 for binary, N for categorical) */
  outcomes: Outcome[];
  /** Current price/probability data */
  pricing:
    | MarketPricing
    | undefined;
  /** Trading volume metrics */
  volume:
    | MarketVolume
    | undefined;
  /** Status and key dates */
  lifecycle:
    | MarketLifecycle
    | undefined;
  /** Trading rules and constraints */
  rules:
    | MarketRules
    | undefined;
  /** Compliance metadata */
  regulatory:
    | MarketRegulatory
    | undefined;
  /** Provider-specific metadata (passthrough for provider extensions) */
  providerMetadata: { [key: string]: string };
}

export interface Market_ProviderMetadataEntry {
  key: string;
  value: string;
}

/** The event being predicted on. */
export interface Event {
  /** Provider's event identifier */
  id: string;
  /** Human-readable title, e.g. "Will Bitcoin reach $100K?" */
  title: string;
  /** Detailed resolution criteria */
  description: string;
  /** Primary category (crypto, politics, sports, etc.) */
  category: string;
  /** Searchable tags */
  tags: string[];
  /** Event image/icon URL */
  imageUrl: string;
  /** For grouped events (e.g. Kalshi "series") */
  seriesId: string;
  /** Series name */
  seriesTitle: string;
}

/** A possible outcome of the market. */
export interface Outcome {
  /** Outcome identifier (e.g. "yes", "no", "candidate_a") */
  id: string;
  /** Human-readable label */
  label: string;
  /** On-chain token ID (if applicable, e.g. Polymarket CTF) */
  tokenId: string;
}

/** Current pricing snapshot for a market. */
export interface MarketPricing {
  /** Last traded prices per outcome */
  lastPrice: { [key: string]: string };
  /** Best bid/ask per outcome */
  bestBid: { [key: string]: string };
  /** outcome_id -> best ask price */
  bestAsk: { [key: string]: string };
  /** Mid price (average of best bid and ask) */
  midPrice: { [key: string]: string };
  /** Spread */
  spread: { [key: string]: string };
  /** Minimum price increment, e.g. "0.01" */
  tickSize: string;
  /** Settlement currency (USD, USDC, etc.) */
  currency: string;
  /** Minimum contracts per order */
  minOrderSize: number;
  /** Maximum contracts per order (0 = unlimited) */
  maxOrderSize: number;
  /** When pricing was last updated */
  updatedAt: Timestamp | undefined;
}

export interface MarketPricing_LastPriceEntry {
  key: string;
  value: string;
}

export interface MarketPricing_BestBidEntry {
  key: string;
  value: string;
}

export interface MarketPricing_BestAskEntry {
  key: string;
  value: string;
}

export interface MarketPricing_MidPriceEntry {
  key: string;
  value: string;
}

export interface MarketPricing_SpreadEntry {
  key: string;
  value: string;
}

/** Trading activity metrics. */
export interface MarketVolume {
  /** All-time volume in settlement currency */
  totalVolume: string;
  /** Last 24h volume */
  volume24h: string;
  /** Last 7d volume */
  volume7d: string;
  /** Currently outstanding contracts */
  openInterest: string;
  /** Unique traders (if available) */
  numTraders: number;
  updatedAt: Timestamp | undefined;
}

/** Market status and key dates. */
export interface MarketLifecycle {
  status: MarketStatus;
  /** When market was created */
  createdAt:
    | Timestamp
    | undefined;
  /** When trading opens (if scheduled) */
  opensAt:
    | Timestamp
    | undefined;
  /** When trading closes */
  closesAt:
    | Timestamp
    | undefined;
  /** When market was resolved (if resolved) */
  resolvedAt:
    | Timestamp
    | undefined;
  /** Hard expiry date */
  expiresAt:
    | Timestamp
    | undefined;
  /** Description of resolution mechanism */
  resolutionSource: string;
}

/** Trading rules and constraints for a market. */
export interface MarketRules {
  allowedOrderTypes: OrderType[];
  allowedTif: TimeInForce[];
  /** Can sell without holding */
  allowsShortSelling: boolean;
  /** Can orders partially fill */
  allowsPartialFill: boolean;
  /** Maker fee as decimal, e.g. "0.00" */
  makerFeeRate: string;
  /** Taker fee as decimal, e.g. "0.02" */
  takerFeeRate: string;
  /** Position limits */
  maxPositionSize: number;
}

/**
 * Compliance and regulatory metadata.
 * Critical for multi-jurisdiction prediction market access.
 */
export interface MarketRegulatory {
  /** Primary jurisdiction (ISO 3166-1 alpha-2) */
  jurisdiction: string;
  /** Provider self-attestation of compliance */
  compliant: boolean;
  /** Regions allowed to trade */
  eligibleRegions: string[];
  /** Regions explicitly blocked */
  restrictedRegions: string[];
  /** Regulatory body (e.g. "CFTC", "none") */
  regulator: string;
  /** License type (e.g. "DCM", "none") */
  licenseType: string;
  /** Legal classification (event_contract, binary_option, swap) */
  contractType: string;
  /** Minimum KYC level required */
  requiredKyc: KycLevel;
}

/** Orderbook snapshot for a specific outcome. */
export interface OrderBook {
  marketId:
    | UniversalMarketId
    | undefined;
  /** Which outcome's book */
  outcomeId: string;
  /** Buy side, sorted price descending */
  bids: OrderBookLevel[];
  /** Sell side, sorted price ascending */
  asks: OrderBookLevel[];
  /** Snapshot timestamp */
  timestamp:
    | Timestamp
    | undefined;
  /**
   * Note: Kalshi only returns bids. UPP adapters must compute asks
   * by inverting the opposing outcome's bids: ask_price = 1 - bid_price
   */
  asksComputed: boolean;
}

/** A single price level in the orderbook. */
export interface OrderBookLevel {
  /** Price at this level (decimal string) */
  price: string;
  /** Number of contracts at this level */
  quantity: number;
}

export interface ListMarketsRequest {
  /** Filters */
  provider: string;
  /** Filter by category */
  category: string;
  /** Filter by status */
  status: MarketStatus;
  /** Filter by type */
  marketType: MarketType;
  /** Filter by tags (AND) */
  tags: string[];
  /** Sorting */
  sortBy: MarketSortField;
  sortDir: SortDirection;
  /** Pagination */
  pagination: PaginationRequest | undefined;
}

export interface ListMarketsResponse {
  markets: Market[];
  pagination: PaginationResponse | undefined;
}

export interface GetMarketRequest {
  /** Provide either universal_id or provider + native_id */
  universalId: string;
  /** Provider domain */
  provider: string;
  /** Provider's native ID */
  nativeId: string;
}

export interface SearchMarketsRequest {
  /** Search query string */
  query: string;
  /** Optional provider filter */
  provider: string;
  /** Optional category filter */
  category: string;
  status: MarketStatus;
  pagination: PaginationRequest | undefined;
}

export interface SearchMarketsResponse {
  markets: Market[];
  pagination: PaginationResponse | undefined;
}

export interface GetOrderBookRequest {
  universalId: string;
  /** Optional: specific outcome. If empty, returns all. */
  outcomeId: string;
  /** Max levels per side (default 20) */
  depth: number;
}

export interface ListCategoriesRequest {
  /** Optional: filter by provider */
  provider: string;
}

export interface ListCategoriesResponse {
  categories: Category[];
}

export interface Category {
  id: string;
  name: string;
  /** Number of open markets in this category */
  marketCount: number;
}

export interface GetRelatedMarketsRequest {
  universalId: string;
  /** Max related markets to return (default 10) */
  limit: number;
}

export interface GetRelatedMarketsResponse {
  markets: RelatedMarket[];
}

export interface RelatedMarket {
  market:
    | Market
    | undefined;
  /** "same_event", "correlated", "opposing" */
  relationship: string;
  /** [-1.0, 1.0] estimated correlation */
  correlationScore: number;
}

function createBaseMarket(): Market {
  return {
    id: undefined,
    event: undefined,
    marketType: 0,
    outcomes: [],
    pricing: undefined,
    volume: undefined,
    lifecycle: undefined,
    rules: undefined,
    regulatory: undefined,
    providerMetadata: {},
  };
}

export const Market: MessageFns<Market> = {
  encode(message: Market, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      UniversalMarketId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.event !== undefined) {
      Event.encode(message.event, writer.uint32(18).fork()).join();
    }
    if (message.marketType !== 0) {
      writer.uint32(24).int32(message.marketType);
    }
    for (const v of message.outcomes) {
      Outcome.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.pricing !== undefined) {
      MarketPricing.encode(message.pricing, writer.uint32(42).fork()).join();
    }
    if (message.volume !== undefined) {
      MarketVolume.encode(message.volume, writer.uint32(50).fork()).join();
    }
    if (message.lifecycle !== undefined) {
      MarketLifecycle.encode(message.lifecycle, writer.uint32(58).fork()).join();
    }
    if (message.rules !== undefined) {
      MarketRules.encode(message.rules, writer.uint32(66).fork()).join();
    }
    if (message.regulatory !== undefined) {
      MarketRegulatory.encode(message.regulatory, writer.uint32(74).fork()).join();
    }
    globalThis.Object.entries(message.providerMetadata).forEach(([key, value]: [string, string]) => {
      Market_ProviderMetadataEntry.encode({ key: key as any, value }, writer.uint32(122).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Market {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = UniversalMarketId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = Event.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.marketType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outcomes.push(Outcome.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pricing = MarketPricing.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.volume = MarketVolume.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lifecycle = MarketLifecycle.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.rules = MarketRules.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.regulatory = MarketRegulatory.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          const entry15 = Market_ProviderMetadataEntry.decode(reader, reader.uint32());
          if (entry15.value !== undefined) {
            message.providerMetadata[entry15.key] = entry15.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Market {
    return {
      id: isSet(object.id) ? UniversalMarketId.fromJSON(object.id) : undefined,
      event: isSet(object.event) ? Event.fromJSON(object.event) : undefined,
      marketType: isSet(object.marketType)
        ? marketTypeFromJSON(object.marketType)
        : isSet(object.market_type)
        ? marketTypeFromJSON(object.market_type)
        : 0,
      outcomes: globalThis.Array.isArray(object?.outcomes) ? object.outcomes.map((e: any) => Outcome.fromJSON(e)) : [],
      pricing: isSet(object.pricing) ? MarketPricing.fromJSON(object.pricing) : undefined,
      volume: isSet(object.volume) ? MarketVolume.fromJSON(object.volume) : undefined,
      lifecycle: isSet(object.lifecycle) ? MarketLifecycle.fromJSON(object.lifecycle) : undefined,
      rules: isSet(object.rules) ? MarketRules.fromJSON(object.rules) : undefined,
      regulatory: isSet(object.regulatory) ? MarketRegulatory.fromJSON(object.regulatory) : undefined,
      providerMetadata: isObject(object.providerMetadata)
        ? (globalThis.Object.entries(object.providerMetadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.provider_metadata)
        ? (globalThis.Object.entries(object.provider_metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Market): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = UniversalMarketId.toJSON(message.id);
    }
    if (message.event !== undefined) {
      obj.event = Event.toJSON(message.event);
    }
    if (message.marketType !== 0) {
      obj.marketType = marketTypeToJSON(message.marketType);
    }
    if (message.outcomes?.length) {
      obj.outcomes = message.outcomes.map((e) => Outcome.toJSON(e));
    }
    if (message.pricing !== undefined) {
      obj.pricing = MarketPricing.toJSON(message.pricing);
    }
    if (message.volume !== undefined) {
      obj.volume = MarketVolume.toJSON(message.volume);
    }
    if (message.lifecycle !== undefined) {
      obj.lifecycle = MarketLifecycle.toJSON(message.lifecycle);
    }
    if (message.rules !== undefined) {
      obj.rules = MarketRules.toJSON(message.rules);
    }
    if (message.regulatory !== undefined) {
      obj.regulatory = MarketRegulatory.toJSON(message.regulatory);
    }
    if (message.providerMetadata) {
      const entries = globalThis.Object.entries(message.providerMetadata) as [string, string][];
      if (entries.length > 0) {
        obj.providerMetadata = {};
        entries.forEach(([k, v]) => {
          obj.providerMetadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<Market>): Market {
    return Market.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Market>): Market {
    const message = createBaseMarket();
    message.id = (object.id !== undefined && object.id !== null) ? UniversalMarketId.fromPartial(object.id) : undefined;
    message.event = (object.event !== undefined && object.event !== null) ? Event.fromPartial(object.event) : undefined;
    message.marketType = object.marketType ?? 0;
    message.outcomes = object.outcomes?.map((e) => Outcome.fromPartial(e)) || [];
    message.pricing = (object.pricing !== undefined && object.pricing !== null)
      ? MarketPricing.fromPartial(object.pricing)
      : undefined;
    message.volume = (object.volume !== undefined && object.volume !== null)
      ? MarketVolume.fromPartial(object.volume)
      : undefined;
    message.lifecycle = (object.lifecycle !== undefined && object.lifecycle !== null)
      ? MarketLifecycle.fromPartial(object.lifecycle)
      : undefined;
    message.rules = (object.rules !== undefined && object.rules !== null)
      ? MarketRules.fromPartial(object.rules)
      : undefined;
    message.regulatory = (object.regulatory !== undefined && object.regulatory !== null)
      ? MarketRegulatory.fromPartial(object.regulatory)
      : undefined;
    message.providerMetadata = (globalThis.Object.entries(object.providerMetadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseMarket_ProviderMetadataEntry(): Market_ProviderMetadataEntry {
  return { key: "", value: "" };
}

export const Market_ProviderMetadataEntry: MessageFns<Market_ProviderMetadataEntry> = {
  encode(message: Market_ProviderMetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Market_ProviderMetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarket_ProviderMetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Market_ProviderMetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Market_ProviderMetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Market_ProviderMetadataEntry>): Market_ProviderMetadataEntry {
    return Market_ProviderMetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Market_ProviderMetadataEntry>): Market_ProviderMetadataEntry {
    const message = createBaseMarket_ProviderMetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEvent(): Event {
  return { id: "", title: "", description: "", category: "", tags: [], imageUrl: "", seriesId: "", seriesTitle: "" };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.category !== "") {
      writer.uint32(34).string(message.category);
    }
    for (const v of message.tags) {
      writer.uint32(42).string(v!);
    }
    if (message.imageUrl !== "") {
      writer.uint32(50).string(message.imageUrl);
    }
    if (message.seriesId !== "") {
      writer.uint32(58).string(message.seriesId);
    }
    if (message.seriesTitle !== "") {
      writer.uint32(66).string(message.seriesTitle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.seriesId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.seriesTitle = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      seriesId: isSet(object.seriesId)
        ? globalThis.String(object.seriesId)
        : isSet(object.series_id)
        ? globalThis.String(object.series_id)
        : "",
      seriesTitle: isSet(object.seriesTitle)
        ? globalThis.String(object.seriesTitle)
        : isSet(object.series_title)
        ? globalThis.String(object.series_title)
        : "",
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.seriesId !== "") {
      obj.seriesId = message.seriesId;
    }
    if (message.seriesTitle !== "") {
      obj.seriesTitle = message.seriesTitle;
    }
    return obj;
  },

  create(base?: DeepPartial<Event>): Event {
    return Event.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Event>): Event {
    const message = createBaseEvent();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.category = object.category ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    message.imageUrl = object.imageUrl ?? "";
    message.seriesId = object.seriesId ?? "";
    message.seriesTitle = object.seriesTitle ?? "";
    return message;
  },
};

function createBaseOutcome(): Outcome {
  return { id: "", label: "", tokenId: "" };
}

export const Outcome: MessageFns<Outcome> = {
  encode(message: Outcome, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    if (message.tokenId !== "") {
      writer.uint32(26).string(message.tokenId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Outcome {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutcome();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Outcome {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      tokenId: isSet(object.tokenId)
        ? globalThis.String(object.tokenId)
        : isSet(object.token_id)
        ? globalThis.String(object.token_id)
        : "",
    };
  },

  toJSON(message: Outcome): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    return obj;
  },

  create(base?: DeepPartial<Outcome>): Outcome {
    return Outcome.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Outcome>): Outcome {
    const message = createBaseOutcome();
    message.id = object.id ?? "";
    message.label = object.label ?? "";
    message.tokenId = object.tokenId ?? "";
    return message;
  },
};

function createBaseMarketPricing(): MarketPricing {
  return {
    lastPrice: {},
    bestBid: {},
    bestAsk: {},
    midPrice: {},
    spread: {},
    tickSize: "",
    currency: "",
    minOrderSize: 0,
    maxOrderSize: 0,
    updatedAt: undefined,
  };
}

export const MarketPricing: MessageFns<MarketPricing> = {
  encode(message: MarketPricing, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.lastPrice).forEach(([key, value]: [string, string]) => {
      MarketPricing_LastPriceEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    globalThis.Object.entries(message.bestBid).forEach(([key, value]: [string, string]) => {
      MarketPricing_BestBidEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    globalThis.Object.entries(message.bestAsk).forEach(([key, value]: [string, string]) => {
      MarketPricing_BestAskEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    globalThis.Object.entries(message.midPrice).forEach(([key, value]: [string, string]) => {
      MarketPricing_MidPriceEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    globalThis.Object.entries(message.spread).forEach(([key, value]: [string, string]) => {
      MarketPricing_SpreadEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    if (message.tickSize !== "") {
      writer.uint32(50).string(message.tickSize);
    }
    if (message.currency !== "") {
      writer.uint32(58).string(message.currency);
    }
    if (message.minOrderSize !== 0) {
      writer.uint32(64).int32(message.minOrderSize);
    }
    if (message.maxOrderSize !== 0) {
      writer.uint32(72).int32(message.maxOrderSize);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(message.updatedAt, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = MarketPricing_LastPriceEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.lastPrice[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = MarketPricing_BestBidEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.bestBid[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = MarketPricing_BestAskEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.bestAsk[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = MarketPricing_MidPriceEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.midPrice[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = MarketPricing_SpreadEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.spread[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.tickSize = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minOrderSize = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.maxOrderSize = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.updatedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing {
    return {
      lastPrice: isObject(object.lastPrice)
        ? (globalThis.Object.entries(object.lastPrice) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.last_price)
        ? (globalThis.Object.entries(object.last_price) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      bestBid: isObject(object.bestBid)
        ? (globalThis.Object.entries(object.bestBid) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.best_bid)
        ? (globalThis.Object.entries(object.best_bid) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      bestAsk: isObject(object.bestAsk)
        ? (globalThis.Object.entries(object.bestAsk) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.best_ask)
        ? (globalThis.Object.entries(object.best_ask) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      midPrice: isObject(object.midPrice)
        ? (globalThis.Object.entries(object.midPrice) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.mid_price)
        ? (globalThis.Object.entries(object.mid_price) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      spread: isObject(object.spread)
        ? (globalThis.Object.entries(object.spread) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      tickSize: isSet(object.tickSize)
        ? globalThis.String(object.tickSize)
        : isSet(object.tick_size)
        ? globalThis.String(object.tick_size)
        : "",
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      minOrderSize: isSet(object.minOrderSize)
        ? globalThis.Number(object.minOrderSize)
        : isSet(object.min_order_size)
        ? globalThis.Number(object.min_order_size)
        : 0,
      maxOrderSize: isSet(object.maxOrderSize)
        ? globalThis.Number(object.maxOrderSize)
        : isSet(object.max_order_size)
        ? globalThis.Number(object.max_order_size)
        : 0,
      updatedAt: isSet(object.updatedAt)
        ? Timestamp.fromJSON(object.updatedAt)
        : isSet(object.updated_at)
        ? Timestamp.fromJSON(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: MarketPricing): unknown {
    const obj: any = {};
    if (message.lastPrice) {
      const entries = globalThis.Object.entries(message.lastPrice) as [string, string][];
      if (entries.length > 0) {
        obj.lastPrice = {};
        entries.forEach(([k, v]) => {
          obj.lastPrice[k] = v;
        });
      }
    }
    if (message.bestBid) {
      const entries = globalThis.Object.entries(message.bestBid) as [string, string][];
      if (entries.length > 0) {
        obj.bestBid = {};
        entries.forEach(([k, v]) => {
          obj.bestBid[k] = v;
        });
      }
    }
    if (message.bestAsk) {
      const entries = globalThis.Object.entries(message.bestAsk) as [string, string][];
      if (entries.length > 0) {
        obj.bestAsk = {};
        entries.forEach(([k, v]) => {
          obj.bestAsk[k] = v;
        });
      }
    }
    if (message.midPrice) {
      const entries = globalThis.Object.entries(message.midPrice) as [string, string][];
      if (entries.length > 0) {
        obj.midPrice = {};
        entries.forEach(([k, v]) => {
          obj.midPrice[k] = v;
        });
      }
    }
    if (message.spread) {
      const entries = globalThis.Object.entries(message.spread) as [string, string][];
      if (entries.length > 0) {
        obj.spread = {};
        entries.forEach(([k, v]) => {
          obj.spread[k] = v;
        });
      }
    }
    if (message.tickSize !== "") {
      obj.tickSize = message.tickSize;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.minOrderSize !== 0) {
      obj.minOrderSize = Math.round(message.minOrderSize);
    }
    if (message.maxOrderSize !== 0) {
      obj.maxOrderSize = Math.round(message.maxOrderSize);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = Timestamp.toJSON(message.updatedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing>): MarketPricing {
    return MarketPricing.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing>): MarketPricing {
    const message = createBaseMarketPricing();
    message.lastPrice = (globalThis.Object.entries(object.lastPrice ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.bestBid = (globalThis.Object.entries(object.bestBid ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.bestAsk = (globalThis.Object.entries(object.bestAsk ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.midPrice = (globalThis.Object.entries(object.midPrice ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.spread = (globalThis.Object.entries(object.spread ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.tickSize = object.tickSize ?? "";
    message.currency = object.currency ?? "";
    message.minOrderSize = object.minOrderSize ?? 0;
    message.maxOrderSize = object.maxOrderSize ?? 0;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null)
      ? Timestamp.fromPartial(object.updatedAt)
      : undefined;
    return message;
  },
};

function createBaseMarketPricing_LastPriceEntry(): MarketPricing_LastPriceEntry {
  return { key: "", value: "" };
}

export const MarketPricing_LastPriceEntry: MessageFns<MarketPricing_LastPriceEntry> = {
  encode(message: MarketPricing_LastPriceEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing_LastPriceEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing_LastPriceEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing_LastPriceEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MarketPricing_LastPriceEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing_LastPriceEntry>): MarketPricing_LastPriceEntry {
    return MarketPricing_LastPriceEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing_LastPriceEntry>): MarketPricing_LastPriceEntry {
    const message = createBaseMarketPricing_LastPriceEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMarketPricing_BestBidEntry(): MarketPricing_BestBidEntry {
  return { key: "", value: "" };
}

export const MarketPricing_BestBidEntry: MessageFns<MarketPricing_BestBidEntry> = {
  encode(message: MarketPricing_BestBidEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing_BestBidEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing_BestBidEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing_BestBidEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MarketPricing_BestBidEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing_BestBidEntry>): MarketPricing_BestBidEntry {
    return MarketPricing_BestBidEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing_BestBidEntry>): MarketPricing_BestBidEntry {
    const message = createBaseMarketPricing_BestBidEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMarketPricing_BestAskEntry(): MarketPricing_BestAskEntry {
  return { key: "", value: "" };
}

export const MarketPricing_BestAskEntry: MessageFns<MarketPricing_BestAskEntry> = {
  encode(message: MarketPricing_BestAskEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing_BestAskEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing_BestAskEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing_BestAskEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MarketPricing_BestAskEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing_BestAskEntry>): MarketPricing_BestAskEntry {
    return MarketPricing_BestAskEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing_BestAskEntry>): MarketPricing_BestAskEntry {
    const message = createBaseMarketPricing_BestAskEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMarketPricing_MidPriceEntry(): MarketPricing_MidPriceEntry {
  return { key: "", value: "" };
}

export const MarketPricing_MidPriceEntry: MessageFns<MarketPricing_MidPriceEntry> = {
  encode(message: MarketPricing_MidPriceEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing_MidPriceEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing_MidPriceEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing_MidPriceEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MarketPricing_MidPriceEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing_MidPriceEntry>): MarketPricing_MidPriceEntry {
    return MarketPricing_MidPriceEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing_MidPriceEntry>): MarketPricing_MidPriceEntry {
    const message = createBaseMarketPricing_MidPriceEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMarketPricing_SpreadEntry(): MarketPricing_SpreadEntry {
  return { key: "", value: "" };
}

export const MarketPricing_SpreadEntry: MessageFns<MarketPricing_SpreadEntry> = {
  encode(message: MarketPricing_SpreadEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketPricing_SpreadEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketPricing_SpreadEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketPricing_SpreadEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MarketPricing_SpreadEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketPricing_SpreadEntry>): MarketPricing_SpreadEntry {
    return MarketPricing_SpreadEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketPricing_SpreadEntry>): MarketPricing_SpreadEntry {
    const message = createBaseMarketPricing_SpreadEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMarketVolume(): MarketVolume {
  return { totalVolume: "", volume24h: "", volume7d: "", openInterest: "", numTraders: 0, updatedAt: undefined };
}

export const MarketVolume: MessageFns<MarketVolume> = {
  encode(message: MarketVolume, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalVolume !== "") {
      writer.uint32(10).string(message.totalVolume);
    }
    if (message.volume24h !== "") {
      writer.uint32(18).string(message.volume24h);
    }
    if (message.volume7d !== "") {
      writer.uint32(26).string(message.volume7d);
    }
    if (message.openInterest !== "") {
      writer.uint32(34).string(message.openInterest);
    }
    if (message.numTraders !== 0) {
      writer.uint32(40).int32(message.numTraders);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(message.updatedAt, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketVolume {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketVolume();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.totalVolume = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.volume24h = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.volume7d = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.openInterest = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.numTraders = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketVolume {
    return {
      totalVolume: isSet(object.totalVolume)
        ? globalThis.String(object.totalVolume)
        : isSet(object.total_volume)
        ? globalThis.String(object.total_volume)
        : "",
      volume24h: isSet(object.volume24h)
        ? globalThis.String(object.volume24h)
        : isSet(object.volume_24h)
        ? globalThis.String(object.volume_24h)
        : "",
      volume7d: isSet(object.volume7d)
        ? globalThis.String(object.volume7d)
        : isSet(object.volume_7d)
        ? globalThis.String(object.volume_7d)
        : "",
      openInterest: isSet(object.openInterest)
        ? globalThis.String(object.openInterest)
        : isSet(object.open_interest)
        ? globalThis.String(object.open_interest)
        : "",
      numTraders: isSet(object.numTraders)
        ? globalThis.Number(object.numTraders)
        : isSet(object.num_traders)
        ? globalThis.Number(object.num_traders)
        : 0,
      updatedAt: isSet(object.updatedAt)
        ? Timestamp.fromJSON(object.updatedAt)
        : isSet(object.updated_at)
        ? Timestamp.fromJSON(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: MarketVolume): unknown {
    const obj: any = {};
    if (message.totalVolume !== "") {
      obj.totalVolume = message.totalVolume;
    }
    if (message.volume24h !== "") {
      obj.volume24h = message.volume24h;
    }
    if (message.volume7d !== "") {
      obj.volume7d = message.volume7d;
    }
    if (message.openInterest !== "") {
      obj.openInterest = message.openInterest;
    }
    if (message.numTraders !== 0) {
      obj.numTraders = Math.round(message.numTraders);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = Timestamp.toJSON(message.updatedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<MarketVolume>): MarketVolume {
    return MarketVolume.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketVolume>): MarketVolume {
    const message = createBaseMarketVolume();
    message.totalVolume = object.totalVolume ?? "";
    message.volume24h = object.volume24h ?? "";
    message.volume7d = object.volume7d ?? "";
    message.openInterest = object.openInterest ?? "";
    message.numTraders = object.numTraders ?? 0;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null)
      ? Timestamp.fromPartial(object.updatedAt)
      : undefined;
    return message;
  },
};

function createBaseMarketLifecycle(): MarketLifecycle {
  return {
    status: 0,
    createdAt: undefined,
    opensAt: undefined,
    closesAt: undefined,
    resolvedAt: undefined,
    expiresAt: undefined,
    resolutionSource: "",
  };
}

export const MarketLifecycle: MessageFns<MarketLifecycle> = {
  encode(message: MarketLifecycle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(message.createdAt, writer.uint32(18).fork()).join();
    }
    if (message.opensAt !== undefined) {
      Timestamp.encode(message.opensAt, writer.uint32(26).fork()).join();
    }
    if (message.closesAt !== undefined) {
      Timestamp.encode(message.closesAt, writer.uint32(34).fork()).join();
    }
    if (message.resolvedAt !== undefined) {
      Timestamp.encode(message.resolvedAt, writer.uint32(42).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(message.expiresAt, writer.uint32(50).fork()).join();
    }
    if (message.resolutionSource !== "") {
      writer.uint32(58).string(message.resolutionSource);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketLifecycle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketLifecycle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.opensAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.closesAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resolvedAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresAt = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resolutionSource = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketLifecycle {
    return {
      status: isSet(object.status) ? marketStatusFromJSON(object.status) : 0,
      createdAt: isSet(object.createdAt)
        ? Timestamp.fromJSON(object.createdAt)
        : isSet(object.created_at)
        ? Timestamp.fromJSON(object.created_at)
        : undefined,
      opensAt: isSet(object.opensAt)
        ? Timestamp.fromJSON(object.opensAt)
        : isSet(object.opens_at)
        ? Timestamp.fromJSON(object.opens_at)
        : undefined,
      closesAt: isSet(object.closesAt)
        ? Timestamp.fromJSON(object.closesAt)
        : isSet(object.closes_at)
        ? Timestamp.fromJSON(object.closes_at)
        : undefined,
      resolvedAt: isSet(object.resolvedAt)
        ? Timestamp.fromJSON(object.resolvedAt)
        : isSet(object.resolved_at)
        ? Timestamp.fromJSON(object.resolved_at)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? Timestamp.fromJSON(object.expiresAt)
        : isSet(object.expires_at)
        ? Timestamp.fromJSON(object.expires_at)
        : undefined,
      resolutionSource: isSet(object.resolutionSource)
        ? globalThis.String(object.resolutionSource)
        : isSet(object.resolution_source)
        ? globalThis.String(object.resolution_source)
        : "",
    };
  },

  toJSON(message: MarketLifecycle): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = marketStatusToJSON(message.status);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = Timestamp.toJSON(message.createdAt);
    }
    if (message.opensAt !== undefined) {
      obj.opensAt = Timestamp.toJSON(message.opensAt);
    }
    if (message.closesAt !== undefined) {
      obj.closesAt = Timestamp.toJSON(message.closesAt);
    }
    if (message.resolvedAt !== undefined) {
      obj.resolvedAt = Timestamp.toJSON(message.resolvedAt);
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = Timestamp.toJSON(message.expiresAt);
    }
    if (message.resolutionSource !== "") {
      obj.resolutionSource = message.resolutionSource;
    }
    return obj;
  },

  create(base?: DeepPartial<MarketLifecycle>): MarketLifecycle {
    return MarketLifecycle.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketLifecycle>): MarketLifecycle {
    const message = createBaseMarketLifecycle();
    message.status = object.status ?? 0;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null)
      ? Timestamp.fromPartial(object.createdAt)
      : undefined;
    message.opensAt = (object.opensAt !== undefined && object.opensAt !== null)
      ? Timestamp.fromPartial(object.opensAt)
      : undefined;
    message.closesAt = (object.closesAt !== undefined && object.closesAt !== null)
      ? Timestamp.fromPartial(object.closesAt)
      : undefined;
    message.resolvedAt = (object.resolvedAt !== undefined && object.resolvedAt !== null)
      ? Timestamp.fromPartial(object.resolvedAt)
      : undefined;
    message.expiresAt = (object.expiresAt !== undefined && object.expiresAt !== null)
      ? Timestamp.fromPartial(object.expiresAt)
      : undefined;
    message.resolutionSource = object.resolutionSource ?? "";
    return message;
  },
};

function createBaseMarketRules(): MarketRules {
  return {
    allowedOrderTypes: [],
    allowedTif: [],
    allowsShortSelling: false,
    allowsPartialFill: false,
    makerFeeRate: "",
    takerFeeRate: "",
    maxPositionSize: 0,
  };
}

export const MarketRules: MessageFns<MarketRules> = {
  encode(message: MarketRules, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.allowedOrderTypes) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.allowedTif) {
      writer.int32(v);
    }
    writer.join();
    if (message.allowsShortSelling !== false) {
      writer.uint32(24).bool(message.allowsShortSelling);
    }
    if (message.allowsPartialFill !== false) {
      writer.uint32(32).bool(message.allowsPartialFill);
    }
    if (message.makerFeeRate !== "") {
      writer.uint32(42).string(message.makerFeeRate);
    }
    if (message.takerFeeRate !== "") {
      writer.uint32(50).string(message.takerFeeRate);
    }
    if (message.maxPositionSize !== 0) {
      writer.uint32(56).int32(message.maxPositionSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketRules {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.allowedOrderTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowedOrderTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.allowedTif.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowedTif.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.allowsShortSelling = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.allowsPartialFill = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.makerFeeRate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.takerFeeRate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxPositionSize = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketRules {
    return {
      allowedOrderTypes: globalThis.Array.isArray(object?.allowedOrderTypes)
        ? object.allowedOrderTypes.map((e: any) => orderTypeFromJSON(e))
        : globalThis.Array.isArray(object?.allowed_order_types)
        ? object.allowed_order_types.map((e: any) => orderTypeFromJSON(e))
        : [],
      allowedTif: globalThis.Array.isArray(object?.allowedTif)
        ? object.allowedTif.map((e: any) => timeInForceFromJSON(e))
        : globalThis.Array.isArray(object?.allowed_tif)
        ? object.allowed_tif.map((e: any) => timeInForceFromJSON(e))
        : [],
      allowsShortSelling: isSet(object.allowsShortSelling)
        ? globalThis.Boolean(object.allowsShortSelling)
        : isSet(object.allows_short_selling)
        ? globalThis.Boolean(object.allows_short_selling)
        : false,
      allowsPartialFill: isSet(object.allowsPartialFill)
        ? globalThis.Boolean(object.allowsPartialFill)
        : isSet(object.allows_partial_fill)
        ? globalThis.Boolean(object.allows_partial_fill)
        : false,
      makerFeeRate: isSet(object.makerFeeRate)
        ? globalThis.String(object.makerFeeRate)
        : isSet(object.maker_fee_rate)
        ? globalThis.String(object.maker_fee_rate)
        : "",
      takerFeeRate: isSet(object.takerFeeRate)
        ? globalThis.String(object.takerFeeRate)
        : isSet(object.taker_fee_rate)
        ? globalThis.String(object.taker_fee_rate)
        : "",
      maxPositionSize: isSet(object.maxPositionSize)
        ? globalThis.Number(object.maxPositionSize)
        : isSet(object.max_position_size)
        ? globalThis.Number(object.max_position_size)
        : 0,
    };
  },

  toJSON(message: MarketRules): unknown {
    const obj: any = {};
    if (message.allowedOrderTypes?.length) {
      obj.allowedOrderTypes = message.allowedOrderTypes.map((e) => orderTypeToJSON(e));
    }
    if (message.allowedTif?.length) {
      obj.allowedTif = message.allowedTif.map((e) => timeInForceToJSON(e));
    }
    if (message.allowsShortSelling !== false) {
      obj.allowsShortSelling = message.allowsShortSelling;
    }
    if (message.allowsPartialFill !== false) {
      obj.allowsPartialFill = message.allowsPartialFill;
    }
    if (message.makerFeeRate !== "") {
      obj.makerFeeRate = message.makerFeeRate;
    }
    if (message.takerFeeRate !== "") {
      obj.takerFeeRate = message.takerFeeRate;
    }
    if (message.maxPositionSize !== 0) {
      obj.maxPositionSize = Math.round(message.maxPositionSize);
    }
    return obj;
  },

  create(base?: DeepPartial<MarketRules>): MarketRules {
    return MarketRules.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketRules>): MarketRules {
    const message = createBaseMarketRules();
    message.allowedOrderTypes = object.allowedOrderTypes?.map((e) => e) || [];
    message.allowedTif = object.allowedTif?.map((e) => e) || [];
    message.allowsShortSelling = object.allowsShortSelling ?? false;
    message.allowsPartialFill = object.allowsPartialFill ?? false;
    message.makerFeeRate = object.makerFeeRate ?? "";
    message.takerFeeRate = object.takerFeeRate ?? "";
    message.maxPositionSize = object.maxPositionSize ?? 0;
    return message;
  },
};

function createBaseMarketRegulatory(): MarketRegulatory {
  return {
    jurisdiction: "",
    compliant: false,
    eligibleRegions: [],
    restrictedRegions: [],
    regulator: "",
    licenseType: "",
    contractType: "",
    requiredKyc: 0,
  };
}

export const MarketRegulatory: MessageFns<MarketRegulatory> = {
  encode(message: MarketRegulatory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jurisdiction !== "") {
      writer.uint32(10).string(message.jurisdiction);
    }
    if (message.compliant !== false) {
      writer.uint32(16).bool(message.compliant);
    }
    for (const v of message.eligibleRegions) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.restrictedRegions) {
      writer.uint32(34).string(v!);
    }
    if (message.regulator !== "") {
      writer.uint32(42).string(message.regulator);
    }
    if (message.licenseType !== "") {
      writer.uint32(50).string(message.licenseType);
    }
    if (message.contractType !== "") {
      writer.uint32(58).string(message.contractType);
    }
    if (message.requiredKyc !== 0) {
      writer.uint32(64).int32(message.requiredKyc);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketRegulatory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketRegulatory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.compliant = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eligibleRegions.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.restrictedRegions.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.regulator = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.licenseType = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.contractType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.requiredKyc = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketRegulatory {
    return {
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
      compliant: isSet(object.compliant) ? globalThis.Boolean(object.compliant) : false,
      eligibleRegions: globalThis.Array.isArray(object?.eligibleRegions)
        ? object.eligibleRegions.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.eligible_regions)
        ? object.eligible_regions.map((e: any) => globalThis.String(e))
        : [],
      restrictedRegions: globalThis.Array.isArray(object?.restrictedRegions)
        ? object.restrictedRegions.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.restricted_regions)
        ? object.restricted_regions.map((e: any) => globalThis.String(e))
        : [],
      regulator: isSet(object.regulator) ? globalThis.String(object.regulator) : "",
      licenseType: isSet(object.licenseType)
        ? globalThis.String(object.licenseType)
        : isSet(object.license_type)
        ? globalThis.String(object.license_type)
        : "",
      contractType: isSet(object.contractType)
        ? globalThis.String(object.contractType)
        : isSet(object.contract_type)
        ? globalThis.String(object.contract_type)
        : "",
      requiredKyc: isSet(object.requiredKyc)
        ? kycLevelFromJSON(object.requiredKyc)
        : isSet(object.required_kyc)
        ? kycLevelFromJSON(object.required_kyc)
        : 0,
    };
  },

  toJSON(message: MarketRegulatory): unknown {
    const obj: any = {};
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    if (message.compliant !== false) {
      obj.compliant = message.compliant;
    }
    if (message.eligibleRegions?.length) {
      obj.eligibleRegions = message.eligibleRegions;
    }
    if (message.restrictedRegions?.length) {
      obj.restrictedRegions = message.restrictedRegions;
    }
    if (message.regulator !== "") {
      obj.regulator = message.regulator;
    }
    if (message.licenseType !== "") {
      obj.licenseType = message.licenseType;
    }
    if (message.contractType !== "") {
      obj.contractType = message.contractType;
    }
    if (message.requiredKyc !== 0) {
      obj.requiredKyc = kycLevelToJSON(message.requiredKyc);
    }
    return obj;
  },

  create(base?: DeepPartial<MarketRegulatory>): MarketRegulatory {
    return MarketRegulatory.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarketRegulatory>): MarketRegulatory {
    const message = createBaseMarketRegulatory();
    message.jurisdiction = object.jurisdiction ?? "";
    message.compliant = object.compliant ?? false;
    message.eligibleRegions = object.eligibleRegions?.map((e) => e) || [];
    message.restrictedRegions = object.restrictedRegions?.map((e) => e) || [];
    message.regulator = object.regulator ?? "";
    message.licenseType = object.licenseType ?? "";
    message.contractType = object.contractType ?? "";
    message.requiredKyc = object.requiredKyc ?? 0;
    return message;
  },
};

function createBaseOrderBook(): OrderBook {
  return { marketId: undefined, outcomeId: "", bids: [], asks: [], timestamp: undefined, asksComputed: false };
}

export const OrderBook: MessageFns<OrderBook> = {
  encode(message: OrderBook, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== undefined) {
      UniversalMarketId.encode(message.marketId, writer.uint32(10).fork()).join();
    }
    if (message.outcomeId !== "") {
      writer.uint32(18).string(message.outcomeId);
    }
    for (const v of message.bids) {
      OrderBookLevel.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.asks) {
      OrderBookLevel.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(message.timestamp, writer.uint32(42).fork()).join();
    }
    if (message.asksComputed !== false) {
      writer.uint32(48).bool(message.asksComputed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderBook {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderBook();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.marketId = UniversalMarketId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outcomeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bids.push(OrderBookLevel.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.asks.push(OrderBookLevel.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timestamp = Timestamp.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.asksComputed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderBook {
    return {
      marketId: isSet(object.marketId)
        ? UniversalMarketId.fromJSON(object.marketId)
        : isSet(object.market_id)
        ? UniversalMarketId.fromJSON(object.market_id)
        : undefined,
      outcomeId: isSet(object.outcomeId)
        ? globalThis.String(object.outcomeId)
        : isSet(object.outcome_id)
        ? globalThis.String(object.outcome_id)
        : "",
      bids: globalThis.Array.isArray(object?.bids) ? object.bids.map((e: any) => OrderBookLevel.fromJSON(e)) : [],
      asks: globalThis.Array.isArray(object?.asks) ? object.asks.map((e: any) => OrderBookLevel.fromJSON(e)) : [],
      timestamp: isSet(object.timestamp) ? Timestamp.fromJSON(object.timestamp) : undefined,
      asksComputed: isSet(object.asksComputed)
        ? globalThis.Boolean(object.asksComputed)
        : isSet(object.asks_computed)
        ? globalThis.Boolean(object.asks_computed)
        : false,
    };
  },

  toJSON(message: OrderBook): unknown {
    const obj: any = {};
    if (message.marketId !== undefined) {
      obj.marketId = UniversalMarketId.toJSON(message.marketId);
    }
    if (message.outcomeId !== "") {
      obj.outcomeId = message.outcomeId;
    }
    if (message.bids?.length) {
      obj.bids = message.bids.map((e) => OrderBookLevel.toJSON(e));
    }
    if (message.asks?.length) {
      obj.asks = message.asks.map((e) => OrderBookLevel.toJSON(e));
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timestamp.toJSON(message.timestamp);
    }
    if (message.asksComputed !== false) {
      obj.asksComputed = message.asksComputed;
    }
    return obj;
  },

  create(base?: DeepPartial<OrderBook>): OrderBook {
    return OrderBook.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OrderBook>): OrderBook {
    const message = createBaseOrderBook();
    message.marketId = (object.marketId !== undefined && object.marketId !== null)
      ? UniversalMarketId.fromPartial(object.marketId)
      : undefined;
    message.outcomeId = object.outcomeId ?? "";
    message.bids = object.bids?.map((e) => OrderBookLevel.fromPartial(e)) || [];
    message.asks = object.asks?.map((e) => OrderBookLevel.fromPartial(e)) || [];
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timestamp.fromPartial(object.timestamp)
      : undefined;
    message.asksComputed = object.asksComputed ?? false;
    return message;
  },
};

function createBaseOrderBookLevel(): OrderBookLevel {
  return { price: "", quantity: 0 };
}

export const OrderBookLevel: MessageFns<OrderBookLevel> = {
  encode(message: OrderBookLevel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.price !== "") {
      writer.uint32(10).string(message.price);
    }
    if (message.quantity !== 0) {
      writer.uint32(16).int64(message.quantity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderBookLevel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderBookLevel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantity = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderBookLevel {
    return {
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
    };
  },

  toJSON(message: OrderBookLevel): unknown {
    const obj: any = {};
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.quantity !== 0) {
      obj.quantity = Math.round(message.quantity);
    }
    return obj;
  },

  create(base?: DeepPartial<OrderBookLevel>): OrderBookLevel {
    return OrderBookLevel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OrderBookLevel>): OrderBookLevel {
    const message = createBaseOrderBookLevel();
    message.price = object.price ?? "";
    message.quantity = object.quantity ?? 0;
    return message;
  },
};

function createBaseListMarketsRequest(): ListMarketsRequest {
  return {
    provider: "",
    category: "",
    status: 0,
    marketType: 0,
    tags: [],
    sortBy: 0,
    sortDir: 0,
    pagination: undefined,
  };
}

export const ListMarketsRequest: MessageFns<ListMarketsRequest> = {
  encode(message: ListMarketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.category !== "") {
      writer.uint32(18).string(message.category);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.marketType !== 0) {
      writer.uint32(32).int32(message.marketType);
    }
    for (const v of message.tags) {
      writer.uint32(42).string(v!);
    }
    if (message.sortBy !== 0) {
      writer.uint32(48).int32(message.sortBy);
    }
    if (message.sortDir !== 0) {
      writer.uint32(56).int32(message.sortDir);
    }
    if (message.pagination !== undefined) {
      PaginationRequest.encode(message.pagination, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMarketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMarketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.marketType = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.sortDir = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.pagination = PaginationRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMarketsRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      status: isSet(object.status) ? marketStatusFromJSON(object.status) : 0,
      marketType: isSet(object.marketType)
        ? marketTypeFromJSON(object.marketType)
        : isSet(object.market_type)
        ? marketTypeFromJSON(object.market_type)
        : 0,
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      sortBy: isSet(object.sortBy)
        ? marketSortFieldFromJSON(object.sortBy)
        : isSet(object.sort_by)
        ? marketSortFieldFromJSON(object.sort_by)
        : 0,
      sortDir: isSet(object.sortDir)
        ? sortDirectionFromJSON(object.sortDir)
        : isSet(object.sort_dir)
        ? sortDirectionFromJSON(object.sort_dir)
        : 0,
      pagination: isSet(object.pagination) ? PaginationRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: ListMarketsRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.status !== 0) {
      obj.status = marketStatusToJSON(message.status);
    }
    if (message.marketType !== 0) {
      obj.marketType = marketTypeToJSON(message.marketType);
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = marketSortFieldToJSON(message.sortBy);
    }
    if (message.sortDir !== 0) {
      obj.sortDir = sortDirectionToJSON(message.sortDir);
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<ListMarketsRequest>): ListMarketsRequest {
    return ListMarketsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListMarketsRequest>): ListMarketsRequest {
    const message = createBaseListMarketsRequest();
    message.provider = object.provider ?? "";
    message.category = object.category ?? "";
    message.status = object.status ?? 0;
    message.marketType = object.marketType ?? 0;
    message.tags = object.tags?.map((e) => e) || [];
    message.sortBy = object.sortBy ?? 0;
    message.sortDir = object.sortDir ?? 0;
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseListMarketsResponse(): ListMarketsResponse {
  return { markets: [], pagination: undefined };
}

export const ListMarketsResponse: MessageFns<ListMarketsResponse> = {
  encode(message: ListMarketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.markets) {
      Market.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PaginationResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMarketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMarketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.markets.push(Market.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PaginationResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMarketsResponse {
    return {
      markets: globalThis.Array.isArray(object?.markets) ? object.markets.map((e: any) => Market.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PaginationResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: ListMarketsResponse): unknown {
    const obj: any = {};
    if (message.markets?.length) {
      obj.markets = message.markets.map((e) => Market.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<ListMarketsResponse>): ListMarketsResponse {
    return ListMarketsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListMarketsResponse>): ListMarketsResponse {
    const message = createBaseListMarketsResponse();
    message.markets = object.markets?.map((e) => Market.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseGetMarketRequest(): GetMarketRequest {
  return { universalId: "", provider: "", nativeId: "" };
}

export const GetMarketRequest: MessageFns<GetMarketRequest> = {
  encode(message: GetMarketRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.universalId !== "") {
      writer.uint32(10).string(message.universalId);
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.nativeId !== "") {
      writer.uint32(26).string(message.nativeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMarketRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMarketRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.universalId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nativeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMarketRequest {
    return {
      universalId: isSet(object.universalId)
        ? globalThis.String(object.universalId)
        : isSet(object.universal_id)
        ? globalThis.String(object.universal_id)
        : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      nativeId: isSet(object.nativeId)
        ? globalThis.String(object.nativeId)
        : isSet(object.native_id)
        ? globalThis.String(object.native_id)
        : "",
    };
  },

  toJSON(message: GetMarketRequest): unknown {
    const obj: any = {};
    if (message.universalId !== "") {
      obj.universalId = message.universalId;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.nativeId !== "") {
      obj.nativeId = message.nativeId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetMarketRequest>): GetMarketRequest {
    return GetMarketRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMarketRequest>): GetMarketRequest {
    const message = createBaseGetMarketRequest();
    message.universalId = object.universalId ?? "";
    message.provider = object.provider ?? "";
    message.nativeId = object.nativeId ?? "";
    return message;
  },
};

function createBaseSearchMarketsRequest(): SearchMarketsRequest {
  return { query: "", provider: "", category: "", status: 0, pagination: undefined };
}

export const SearchMarketsRequest: MessageFns<SearchMarketsRequest> = {
  encode(message: SearchMarketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.category !== "") {
      writer.uint32(26).string(message.category);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.pagination !== undefined) {
      PaginationRequest.encode(message.pagination, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchMarketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchMarketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.pagination = PaginationRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchMarketsRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      status: isSet(object.status) ? marketStatusFromJSON(object.status) : 0,
      pagination: isSet(object.pagination) ? PaginationRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: SearchMarketsRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.status !== 0) {
      obj.status = marketStatusToJSON(message.status);
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchMarketsRequest>): SearchMarketsRequest {
    return SearchMarketsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchMarketsRequest>): SearchMarketsRequest {
    const message = createBaseSearchMarketsRequest();
    message.query = object.query ?? "";
    message.provider = object.provider ?? "";
    message.category = object.category ?? "";
    message.status = object.status ?? 0;
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseSearchMarketsResponse(): SearchMarketsResponse {
  return { markets: [], pagination: undefined };
}

export const SearchMarketsResponse: MessageFns<SearchMarketsResponse> = {
  encode(message: SearchMarketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.markets) {
      Market.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PaginationResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchMarketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchMarketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.markets.push(Market.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PaginationResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchMarketsResponse {
    return {
      markets: globalThis.Array.isArray(object?.markets) ? object.markets.map((e: any) => Market.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PaginationResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: SearchMarketsResponse): unknown {
    const obj: any = {};
    if (message.markets?.length) {
      obj.markets = message.markets.map((e) => Market.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PaginationResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchMarketsResponse>): SearchMarketsResponse {
    return SearchMarketsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchMarketsResponse>): SearchMarketsResponse {
    const message = createBaseSearchMarketsResponse();
    message.markets = object.markets?.map((e) => Market.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PaginationResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseGetOrderBookRequest(): GetOrderBookRequest {
  return { universalId: "", outcomeId: "", depth: 0 };
}

export const GetOrderBookRequest: MessageFns<GetOrderBookRequest> = {
  encode(message: GetOrderBookRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.universalId !== "") {
      writer.uint32(10).string(message.universalId);
    }
    if (message.outcomeId !== "") {
      writer.uint32(18).string(message.outcomeId);
    }
    if (message.depth !== 0) {
      writer.uint32(24).int32(message.depth);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetOrderBookRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOrderBookRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.universalId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outcomeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.depth = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOrderBookRequest {
    return {
      universalId: isSet(object.universalId)
        ? globalThis.String(object.universalId)
        : isSet(object.universal_id)
        ? globalThis.String(object.universal_id)
        : "",
      outcomeId: isSet(object.outcomeId)
        ? globalThis.String(object.outcomeId)
        : isSet(object.outcome_id)
        ? globalThis.String(object.outcome_id)
        : "",
      depth: isSet(object.depth) ? globalThis.Number(object.depth) : 0,
    };
  },

  toJSON(message: GetOrderBookRequest): unknown {
    const obj: any = {};
    if (message.universalId !== "") {
      obj.universalId = message.universalId;
    }
    if (message.outcomeId !== "") {
      obj.outcomeId = message.outcomeId;
    }
    if (message.depth !== 0) {
      obj.depth = Math.round(message.depth);
    }
    return obj;
  },

  create(base?: DeepPartial<GetOrderBookRequest>): GetOrderBookRequest {
    return GetOrderBookRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetOrderBookRequest>): GetOrderBookRequest {
    const message = createBaseGetOrderBookRequest();
    message.universalId = object.universalId ?? "";
    message.outcomeId = object.outcomeId ?? "";
    message.depth = object.depth ?? 0;
    return message;
  },
};

function createBaseListCategoriesRequest(): ListCategoriesRequest {
  return { provider: "" };
}

export const ListCategoriesRequest: MessageFns<ListCategoriesRequest> = {
  encode(message: ListCategoriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCategoriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCategoriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCategoriesRequest {
    return { provider: isSet(object.provider) ? globalThis.String(object.provider) : "" };
  },

  toJSON(message: ListCategoriesRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create(base?: DeepPartial<ListCategoriesRequest>): ListCategoriesRequest {
    return ListCategoriesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCategoriesRequest>): ListCategoriesRequest {
    const message = createBaseListCategoriesRequest();
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseListCategoriesResponse(): ListCategoriesResponse {
  return { categories: [] };
}

export const ListCategoriesResponse: MessageFns<ListCategoriesResponse> = {
  encode(message: ListCategoriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.categories) {
      Category.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCategoriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCategoriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.categories.push(Category.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCategoriesResponse {
    return {
      categories: globalThis.Array.isArray(object?.categories)
        ? object.categories.map((e: any) => Category.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListCategoriesResponse): unknown {
    const obj: any = {};
    if (message.categories?.length) {
      obj.categories = message.categories.map((e) => Category.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListCategoriesResponse>): ListCategoriesResponse {
    return ListCategoriesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCategoriesResponse>): ListCategoriesResponse {
    const message = createBaseListCategoriesResponse();
    message.categories = object.categories?.map((e) => Category.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCategory(): Category {
  return { id: "", name: "", marketCount: 0 };
}

export const Category: MessageFns<Category> = {
  encode(message: Category, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.marketCount !== 0) {
      writer.uint32(24).int32(message.marketCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Category {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCategory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.marketCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Category {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      marketCount: isSet(object.marketCount)
        ? globalThis.Number(object.marketCount)
        : isSet(object.market_count)
        ? globalThis.Number(object.market_count)
        : 0,
    };
  },

  toJSON(message: Category): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.marketCount !== 0) {
      obj.marketCount = Math.round(message.marketCount);
    }
    return obj;
  },

  create(base?: DeepPartial<Category>): Category {
    return Category.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Category>): Category {
    const message = createBaseCategory();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.marketCount = object.marketCount ?? 0;
    return message;
  },
};

function createBaseGetRelatedMarketsRequest(): GetRelatedMarketsRequest {
  return { universalId: "", limit: 0 };
}

export const GetRelatedMarketsRequest: MessageFns<GetRelatedMarketsRequest> = {
  encode(message: GetRelatedMarketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.universalId !== "") {
      writer.uint32(10).string(message.universalId);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRelatedMarketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRelatedMarketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.universalId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRelatedMarketsRequest {
    return {
      universalId: isSet(object.universalId)
        ? globalThis.String(object.universalId)
        : isSet(object.universal_id)
        ? globalThis.String(object.universal_id)
        : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GetRelatedMarketsRequest): unknown {
    const obj: any = {};
    if (message.universalId !== "") {
      obj.universalId = message.universalId;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<GetRelatedMarketsRequest>): GetRelatedMarketsRequest {
    return GetRelatedMarketsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetRelatedMarketsRequest>): GetRelatedMarketsRequest {
    const message = createBaseGetRelatedMarketsRequest();
    message.universalId = object.universalId ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetRelatedMarketsResponse(): GetRelatedMarketsResponse {
  return { markets: [] };
}

export const GetRelatedMarketsResponse: MessageFns<GetRelatedMarketsResponse> = {
  encode(message: GetRelatedMarketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.markets) {
      RelatedMarket.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRelatedMarketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRelatedMarketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.markets.push(RelatedMarket.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRelatedMarketsResponse {
    return {
      markets: globalThis.Array.isArray(object?.markets)
        ? object.markets.map((e: any) => RelatedMarket.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetRelatedMarketsResponse): unknown {
    const obj: any = {};
    if (message.markets?.length) {
      obj.markets = message.markets.map((e) => RelatedMarket.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetRelatedMarketsResponse>): GetRelatedMarketsResponse {
    return GetRelatedMarketsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetRelatedMarketsResponse>): GetRelatedMarketsResponse {
    const message = createBaseGetRelatedMarketsResponse();
    message.markets = object.markets?.map((e) => RelatedMarket.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRelatedMarket(): RelatedMarket {
  return { market: undefined, relationship: "", correlationScore: 0 };
}

export const RelatedMarket: MessageFns<RelatedMarket> = {
  encode(message: RelatedMarket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.market !== undefined) {
      Market.encode(message.market, writer.uint32(10).fork()).join();
    }
    if (message.relationship !== "") {
      writer.uint32(18).string(message.relationship);
    }
    if (message.correlationScore !== 0) {
      writer.uint32(29).float(message.correlationScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelatedMarket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelatedMarket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.market = Market.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relationship = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.correlationScore = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelatedMarket {
    return {
      market: isSet(object.market) ? Market.fromJSON(object.market) : undefined,
      relationship: isSet(object.relationship) ? globalThis.String(object.relationship) : "",
      correlationScore: isSet(object.correlationScore)
        ? globalThis.Number(object.correlationScore)
        : isSet(object.correlation_score)
        ? globalThis.Number(object.correlation_score)
        : 0,
    };
  },

  toJSON(message: RelatedMarket): unknown {
    const obj: any = {};
    if (message.market !== undefined) {
      obj.market = Market.toJSON(message.market);
    }
    if (message.relationship !== "") {
      obj.relationship = message.relationship;
    }
    if (message.correlationScore !== 0) {
      obj.correlationScore = message.correlationScore;
    }
    return obj;
  },

  create(base?: DeepPartial<RelatedMarket>): RelatedMarket {
    return RelatedMarket.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RelatedMarket>): RelatedMarket {
    const message = createBaseRelatedMarket();
    message.market = (object.market !== undefined && object.market !== null)
      ? Market.fromPartial(object.market)
      : undefined;
    message.relationship = object.relationship ?? "";
    message.correlationScore = object.correlationScore ?? 0;
    return message;
  },
};

export interface MarketServiceImplementation<CallContextExt = {}> {
  /** List markets with filtering, sorting, and pagination. */
  listMarkets(
    request: ListMarketsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListMarketsResponse>>;
  /** Get detailed metadata for a single market. */
  getMarket(request: GetMarketRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Market>>;
  /** Full-text and semantic search across all providers. */
  searchMarkets(
    request: SearchMarketsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SearchMarketsResponse>>;
  /** Get the current orderbook for a market outcome. */
  getOrderBook(request: GetOrderBookRequest, context: CallContext & CallContextExt): Promise<DeepPartial<OrderBook>>;
  /** List available market categories. */
  listCategories(
    request: ListCategoriesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListCategoriesResponse>>;
  /** Get a snapshot of related/correlated markets. */
  getRelatedMarkets(
    request: GetRelatedMarketsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetRelatedMarketsResponse>>;
}

export interface MarketServiceClient<CallOptionsExt = {}> {
  /** List markets with filtering, sorting, and pagination. */
  listMarkets(
    request: DeepPartial<ListMarketsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListMarketsResponse>;
  /** Get detailed metadata for a single market. */
  getMarket(request: DeepPartial<GetMarketRequest>, options?: CallOptions & CallOptionsExt): Promise<Market>;
  /** Full-text and semantic search across all providers. */
  searchMarkets(
    request: DeepPartial<SearchMarketsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SearchMarketsResponse>;
  /** Get the current orderbook for a market outcome. */
  getOrderBook(request: DeepPartial<GetOrderBookRequest>, options?: CallOptions & CallOptionsExt): Promise<OrderBook>;
  /** List available market categories. */
  listCategories(
    request: DeepPartial<ListCategoriesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListCategoriesResponse>;
  /** Get a snapshot of related/correlated markets. */
  getRelatedMarkets(
    request: DeepPartial<GetRelatedMarketsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetRelatedMarketsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
